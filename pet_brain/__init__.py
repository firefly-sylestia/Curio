"""Trainable artificial pet brain package for Curio.

The package is deliberately separate from the Android runtime.  It contains
training, evaluation, and export tooling; the app integration can consume a
verified exported model later.
"""

__version__ = "0.1.0"
