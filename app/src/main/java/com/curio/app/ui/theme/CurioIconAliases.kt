package com.curio.app.ui.theme

/**
 * Compatibility aliases for icon names used by newer UI surfaces.
 * Keep these mapped to glyphs already exposed by CurioIcons so the
 * bundled Material Symbols subset remains the single source of rendering.
 */
val CurioIcons.ChevronUp: String
    get() = CurioIcons.KeyboardArrowUp

val CurioIcons.ChevronDown: String
    get() = CurioIcons.KeyboardArrowDown
