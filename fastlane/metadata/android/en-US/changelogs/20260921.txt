ADD: Settings — the top nav rail (All Settings / Appearance / Pet designer / Preferences / Recording / Categories / Topic history / Share hub / Experiments / Backup / Support) now rides EVERY settings page, not just the hub: the page you're on sits highlighted in the second slot (right after All Settings), every other section is one tap away, and switching REPLACES the current page so the hub stays one back-press away. Drill-in pages (Book covers, Book browser, Recycle bin, Updates, Widget editor) carry the same bar for switching back out.
FIX: Share-card inline editor — the cursor is now ACCURATE when you tap to edit (bottom-sheet preview AND full screen): the hidden typing field tracks the card's REAL current text size on every change (the old report could go stale when the smart fit shrank the text inside a clipped box, so the caret landed off the letters), and formatted runs (bold / italic / per-run font sizes) wrap exactly like the visible text.
FIX: Share-card smart fit — long facts now REALLY fit: the wrap measurement uses the real card width (the old design-width measure cut long facts by ~1.5×, worst on the Clean 9:16 card), the box grows to the FULL free middle on 9:16 (Clean up to ~2.9×) with the text sized to match, and a new height bound guarantees the "via Curio" footer is never pushed or hidden.
FIX: Share-card auto-layout pill — when a 3:4 card genuinely can't hold the text, the very next sparkle tap AUTO-FLIPS the card to 9:16 with a properly fitted tall layout (no more cycling through five cramped arrangements first).
FIX: Share-card sliders — the smart-fit size is now always VISIBLE in the sliders: the text thumb never hides a smaller rendered size (the fit stops at the slider's 0.5× floor), and dragging the Fact-height slider no longer collapses the box (it captures the fitted height and text size seamlessly).
ADD: Settings — the whole hub is redesigned (the CurioSettings_Redesign JSX): a nav rail (All Settings / Appearance / Pet / Preferences / Recording / Categories / History / Share / Experiments / Backup / Support), a search box, and grouped TONE CARDS — Personalize, How it works, Organize your world, Share & explore, and Your data & privacy (Backup & restore + Book covers) — each card a pastel gradient with a frosted icon tile, round arrow and a decorative foot visual, plus secondary cards (Recycle bin, Updates, Help & feedback) and a footer note. The header stays the app's own, and every card opens the real settings screen.
ADD: Cabinet — liked books/albums/series now wear their OWN color: the DOMINANT color is extracted from each cover's cached art and used for the rows, tiles, cover plates and the Everything preview (fallback = category accent while a cover is still downloading), and saved REVIEWS borrow the reviewed media's cover color too.
FIX: Cabinet — the thick outline on review cards is gone (the extracted cover color tints the card instead), and opening a collection / shelf / Everything now opens from the TOP — each level gets a fresh grid scroll, so switching pages no longer jumps or glitches mid-list.
ADD: Cabinet — collection cards are fully designed now: each folder wears its art as the CARD BACKGROUND (Favorites a star-map, Currently Reading an open book, Want to Read stacked spines, Completed a redesigned sun-summit with a planted flag, Notes a slip-stack with a pen, Personal a moonlit window, Saved a photo collage) plus four new MINIMAL line-art scenes for user collections.
ADD: Cabinet — the New-collection sheet now picks your card style: tone swatches, live art previews and icon chips (each optional — Auto cycles the palette), saved per collection.
FIX: Cabinet — the ⋮ on collection cards and inside a collection is an ANCHORED dropdown now (Rename / Add captures / Delete) instead of the center-screen overlay, and the "tap a member to open it" hint text is gone.
FIX: Share-card editor — the typing field now hugs the VISIBLE fact box exactly: the caret can no longer sit below the text you see, and the selection outline no longer extends past the box (bottom-sheet preview + full-screen card editor).
FIX: Share-card smart fit — the fact box now EXPANDS to its full design height whenever the text is longer (not just smaller text), so a long fact looks taller when the card has room; a quick-fact box you dragged onto the title/info rows (or off the card) auto-moves back to its proper place on the sparkle tap; the sparkle can never push the title up over the CATEGORY ICON anymore; and the "via Curio" footers are smaller and stay put.
ADD: Cabinet — liked books/albums/series now keep a separate cover CACHE on device: the resolved art (URL + image bytes) is warmed automatically and saved to disk, so the Cabinet fills with real covers instantly, works offline, and stays cached forever.
ADD: Cabinet — the Everything library is a 3-column media grid with a DYNAMIC view per kind: Books wear portrait jackets, Albums square covers with a vinyl-disc hint, Series posters, and saved reviews get their own OUTLINED card with star rating and preview — no more one-look-alike tiles. The list view is gone (the grid replaced it), and the filter rail only shows types that actually have something in them.
ADD: Cabinet — the Recent rail now mixes your latest captures with recently LIKED books, albums and series (new liked timestamps), newest first.
FIX: Cabinet — the Everything preview card is theme-aware now (the hardcoded cream wash is gone), shows ONE arrow (the duplicate arrow and the '+' slot were removed), and its cover rail scrolls so every saved cover is reachable.
FIX: Cabinet — back button walks OUT properly: from a collection, shelf or Everything it goes back to the Cabinet home instead of jumping to the Home tab.
FIX: Cabinet — the ⋮ on collection cards finally does something (rename / delete, same menu as long-press); the Completed shelf icon is a crisp check mark now (the old glyph read squished); and every liked tile's ⋮ opens a Cover source sheet to switch to the item's OTHER art provider — re-cached instantly.
FIX: Text history — the clock pill + auto-save now cover EVERY text field: the share-card TITLE joins the feed (quick fact, custom fact, chapter review, quote, photo caption and title are all captured), and the chapter-note Enlarge editor got its own pill + capture too — the pill floats ABOVE the keyboard in every editor.
FIX: Text history — no more duplicate snapshots: retyping/restoring the same text as an older entry MOVES that entry to the top instead of stacking a repeat, and a brand-new TREE view groups snapshots that share paragraphs into branches — the shared opening is the trunk and each version shows only its CHANGED lines (removed struck through, added highlighted), tree-style.
FIX: Text history — the browser is a proper bottom sheet now (drag handle, swipe to dismiss) with a List / Tree toggle, and the PIN button finally works: it was disabled on unpinned entries (you could only ever unpin) — any snapshot can now be pinned to the top.
FIX: Home — holding a recents row opens the category picker's ANCHORED action menu (pops in at your finger, stays open after release, scrim + idle auto-dismiss) instead of the old centered dialog.
FIX: Full-screen editors — the share-card Enlarge + full-screen card editor + the chapter-note Enlarge all lift ABOVE the keyboard (imePadding) with a scrollable text area, so a long note is always reachable and selectable without closing the keyboard.
FIX: Rich-text tools — applying Bold / Italic / Highlight / letter size to a SELECTION is one-shot now: the tool turns off after that single change instead of staying lit and sticking to everything typed next (tapping a tool with no selection still arms it).
FIX: Series — every series topic now shows its section on the reveal page: series without an episode guide still get the poster + synopsis card (they used to show nothing at all), and series with a guide gain an EPISODES chip row (S1E1 key + title) that jumps the episode-list sheet straight to that episode — the sheet, its poster card and the chips all wear the TV/clapperboard glyph, never the book icon.
FIX: Notes sheets in DARK mode — the sheet action icons are visible again: the favorite hearts (book chapters, album tracks, series episodes), the read/watched toggles, the note chips and the album LISTEN dropdown glyphs now resolve the cover's light ink twin instead of the dim palette tone that vanished into the tinted dark wash. The chapter "Add a note" field icons lift to full strength too. Light mode is unchanged.
FIX: Build — the Cabinet shelf-art cards compiled again: the decorative art composables (stars, open book, books, mountain, notes, window, photos) are Box-scoped so their alignments resolve, and the book-spine widths use Int math before converting to dp.
CHANGE: Glass toolbar header — the smooth morph-collapse is PROFILE only now: the Profile header expands with your avatar + streak + Edit pills at the top and collapses to the slim identity bar (avatar + name + streak + Edit) as you scroll, while HOME keeps its static glass toolbar and floating menu/avatar pills exactly as before — the pinned collapsing bar on Home was covering the pet while it wandered.
FIX: Share-card editor — the smart fit NEVER cuts or overlaps anymore: when a long fact's box can't grow any further, the TEXT keeps shrinking (down to a readable hard floor) until the whole fact fits — long facts no longer ellipsize or get cut, and the box never grows into the footer or off the card.
FIX: Share-card editor — the auto-layout sparkle now returns a title you dragged over the quick fact all the way back to the top: the manual drag is reset to its natural spot in one tap (it used to settle for a partial lift that could leave it inside the fact).
FIX: Collage card — the tilted cream polaroid no longer shows a dark shadow/background behind it: the print's drop shadow is off on the collage (the tape + tilt keep its scrapbook depth), so the cream strip reads clean in the preview AND the exported image.
FIX: Share-card editor — smart fit now MEASURES the fact text's real wrapped line count (TextMeasurer at the design width) instead of guessing from character count, so a long URL or a run of long words fits correctly: text-first, the type shrinks then the box grows only up to the design's budget.
FIX: Share-card editor — the auto-layout sparkle pill now lifts the title OUT of the grown quick-fact area in one tap: on bottom-anchored cards (Clean/Minimal) the grown fact box's rise is computed from the measured boxes and the title is lifted by exactly the overlap (the old per-step guess left the title sitting inside the fact until a second tap).
ADD: Headers — a new "Glass toolbar header" style (Settings → Experiments → Headers) swaps the torn paper banners for a content-height liquid-glass bar — more blur, its own rose tint — across Settings, Cabinet, Home and Profile (Spin keeps its own chrome).
FIX: Share-card editor — the polaroid's film-window outline is now ACCURATE: the hairline hugs the window from INSIDE (it used to straddle the edge and get washed out by the gloss, reading as a broken dark square), the photo window is clipped to matching corners, and the finish adapts to the photo filter (B&W turns the line gray, Nostalgia/Warm warm it) with a per-style stroke (Vintage thinner + fainter, Noir bolder) — the Dashed style keeps its dotted hairline so it stays easy to identify.
FIX: Share-card editor — the polaroid print is never cut off on other card styles now: the caption band grows to fit the handwritten name on narrow/small prints (the old proportional band let the caption spill past the print's bottom edge), and the print keeps a small top inset so the washi tape isn't sliced at the card's edge.
FIX: Cabinet — first-open jank/heat on the saved-captures grid: every card stopped re-allocating its header gradient brush on each recomposition (the cache now keys on the actual colors), so the grid settles without the per-card allocation churn that lagged and heated the screen.
FIX: Saved entries — opening an entry no longer collects and scans the WHOLE captures table on every database change: the detail page now observes the single row by id (Room primary-key query), so it opens instantly and edits reflect without the full-table decode.
REMOVE: Experiments — the concluded experiments were removed and their winners hardcoded: the classic category picker (the new picker is the only picker), promo/demo mode (the app always shows real data), real blur on older devices + the custom blur engine (old devices get the static veil, widgets use system blur), the glass widget lab (its clock + streak home-screen widgets are gone; the tile widget + editor stay), and the subtle pill glow + live explore notification toggles (both are now always on).
ADD: Cabinet — the experimental v2 view is now the FOLDERS design: an Everything card with a strip of your REAL saved covers (books, albums, series) and an arrow into the Everything library; your Saved entries below it; then a Collections grid of built-in shelves — Favorites (your liked books/series/albums), Currently Reading, Want to Read, Saved entries, Completed, Notes and Personal — plus your own collections and a + New collection tile. Every collection card wears the JSX style: pastel tone, glyph, item count and decorative art, theme-aware in dark mode. Three suggested discoveries with a Shuffle pill still greet a genuinely empty Cabinet.
ADD: Cabinet — the Everything library is rebuilt: Filter + Sort pills, a grid/list view toggle, a type filter rail (All · Books · Albums · Series · Notes · Moodboards · Reviews), a Recent rail, and an "Add something new" button that opens Spin to explore and save. Saved captures keep the classic card; liked items render as jacket-art tiles.
ADD: Cabinet — the four starter shelves (Currently Reading / Want to Read / Completed / Personal) are created once and behave like any collection — add captures, file topics from the reveal, rename, delete — while Favorites / Saved entries / Notes are live views of your real data.
ADD: Cabinet — COLLECTIONS (folders) in the experimental v2 view: create, rename and delete collections; open one to see its members (saved captures as cards, pinned discoveries as rows) with long-press Move up / Move down / Remove and reordering that changes the folder's cover; add saved captures via a multi-select sheet, or create a collection straight from a MOODBOARD (board metaphor preserved).
ADD: Topic Reveal — hold the top bar to pin a discovery into a collection ("File to collection…"): the picker lists your collections (already-filed ones wear a check), and a create-on-the-spot path files the topic into a brand-new folder. No duplicates are ever written.
CHANGE: Recents — tapping a recent row now keeps the TOPIC open (the reveal page, where you can write, save or keep exploring) instead of jumping to the entry or the write flow; long-press a row for the other actions (write about it, open the saved entry, remove from Recents) — on the Recents page and the Home preview.
ADD: Book browser — matches the app's style now: the settings-family torn rose hero with a real search in the banner, liquid-glass pills, and the same list-first layout on wide windows as the other settings pages.
FIX: Cabinet v2 — liked books, series and albums now open their REAL topic: "Animal Farm" the BOOK no longer opens the animated film of the same name (books resolve from the Books lane first, series from Series, albums from Albums — the lane where you actually hearted them), and the rows open reliably right after a restart even while the topic catalog is still loading (they fall back to the book/album/series lane instead of silently doing nothing).
FIX: Cabinet v2 — the category label on liked rows uses readable ink now: the old category-coloured text blended into the tinted row background and washed out.
FIX: Share-card editor — the POLAROID now works on EVERY card style from the start: the Polaroid tool is available on all designs (not just the collage), and switching on "Show on card" shows the print even before a photo is picked — an empty frame with a "Tap to add photo" hint opens the picker, exactly like the collage.
ADD: Cabinet — the experimental Cabinet v2 collections view gains three polish touches: liked albums/series rows show the artwork you already resolved on their pages (instant covers, no re-fetch), an EMPTY Cabinet suggests three discoveries with a Shuffle pill instead of a blank page, and saved captures filter by capture format (Voice / Review / Journal / Moodboard / Field notes / Wildcard chips).
CHANGE: Share-card editor — the polaroid print on non-Collage styles is now OPT-IN (never always-on): a "Show on card" switch in the Polaroid panel (bottom sheet and full screen) turns the print on per card, and the same switch hides it again — Collage keeps its own built-in print.
ADD: Cabinet — the experimental CABINET V2 collections view (Settings → Experiments → "Cabinet v2 collections", off by default): a glass toolbar with search sits over one grouped page of your SAVED captures plus your LIKED BOOKS, LIKED SERIES and LIKED ALBUMS — each liked row shows contain-fit jacket art (books as a half-book with a spine + sheen, albums square, series as a poster — never stretched), sections collapse on tap, and saved captures keep the long-press multi-select + batch move-to-recycle-bin flow. While it's on, Home's Cabinet shortcut lands here; flipping it off restores the classic view untouched.
FIX: Share-card editor — the Minimal card's favorite-tracks strip is now a real LIST (one quiet track per line with a dot, instead of one dot-joined line that hid the names), and the smart fit / default fit now keep the quick-fact box clear of it: the fact's bottom edge reserves the strip's slot, so a grown fact can never sit over the list's lower lines (and the sparkle has nothing left to repair).
ADD: Share-card editor — the POLAROID now works on EVERY card style, not just the collage: once a photo is on the card the print appears on any design with the same frame styles, filters and size controls, and its editor panel (Style · Photo filter · Print size) now lives in the bottom-sheet toolbar too — tap the print on the card and the panel opens by itself, full screen included.
CHANGE: Share-card editor — the polaroid gained two new looks (CANDY — rose paper + blush tape, and NOIR — dark charcoal print + silver tape) and the old SUNGLOW style is reworked: the flat butter-yellow fill that read like a cheap sticker is now warm ivory paper with a honey tape that actually contrasts.
FIX: Share-card editor — the auto-layout sparkle's overlap fix is now a proper three-step solver: the title lifts, then the fact pushes DOWN (never past the card's bottom edge), and any leftover overlap SHRINKS the fact box — so a tall grown fact can never leave a stuck overlap on the title again. Smart fit also grows Paper's box a little further before the text floor kicks in.
FIX: Share-card editor — the full-screen preview no longer shrinks and re-flows when a tool panel opens below: the Text / Stickers / Polaroid panels now float OVER the card, so the preview keeps its exact size, text wrap and look while you edit.
FIX: Share-card editor — emoji stickers stay tappable after the Stickers panel closes (a placed emoji overlapping a box could previously never be selected again), and tapping a box to select it no longer flashes a ripple.
FIX: Share-card editor — widening the quick-fact box now CENTERS a short fact inside it (it used to hug the box's left edge with a gap); the box itself still expands into the side gutters.
FIX: Collage card — the favorites strip no longer straddles the torn seam (its last track row used to slide into the dark band and split the text across the two collage tones): it sits fully on the cream paper now, and the torn seam itself rises a touch so the dark band owns more of the card.
FIX: Paper card — the footer is smaller and sits clear of the torn bottom strip: it no longer hides behind the bottom decoration or gets pushed off the card by a grown fact box.
FIX: Share-card editor — the auto-layout sparkle now respects the CATEGORY PILL: the lifted title stops at the pill instead of sliding over it, a grown quick-fact box no longer buries it, a pill dragged onto the title snaps back up, and a pill dragged off the card edge is pulled back inside — and the one-tap repair works on the resting preview too, not just while editing.
CHANGE: Share-card editor — the full-screen editor's top buttons (Close, card size, Reset layout, Stickers, Polaroid, Text) are ICON-ONLY now, so they never crowd or overlap on narrow screens, and the tool panels slide open smoothly at the bottom.
FIX: Share-card editor — the collage polaroid is no longer sliced by its own selection outline (the border now floats clear of the print and its tape), and the print-size slider actually grows the print across its whole range again (it used to stop after ~1×).
FIX: Share-card editor — tapping the card OUTSIDE a selected sticker now deselects it (the handles disappear) instead of staying stuck selected.
ADD: Share-card editor — TEXT HISTORY: every edit you type, paste or clear (quick fact, custom fact, chapter review, photo caption…) is snapshotted when you pause, at every 10th word, or when you leave the editor — a clock pill in the sheet corner, the full-screen editor and the writing sheet opens the history, where each snapshot shows its field + time and can be PINNED, COPIED, RESTORED back into the field or deleted. History is stored on-device and never clears on Reset layout or a new card.
FIX: Share-card editor — the Underline format icon renders properly again (the glyph name it pointed at doesn't exist in the icon font, so it showed as raw text) and the share dialog's link icon is bundled too.
FIX: Share-card editor — the last batch's build errors are fixed (polaroid filter colours, the polaroid canvas height and sticker pinch/rotate on the card).
ADD: Share-card editor — import your OWN PNG cutouts as stickers: an "Import PNG cutout" button in the Stickers panel copies your image (transparency kept) onto the card, where it drags, resizes, re-stacks and saves like any emoji sticker.
ADD: Share-card editor — stickers can be ROTATED: a Rotation slider in the sticker panel, or twist two fingers on the card; the emoji picker is no longer the only sticker source.
CHANGE: Share-card editor — the full-screen tools moved to the BOTTOM of the editor (Text, Stickers and Polaroid panels now sit under the card, under your thumb), and the sheet's toolbar pills are ICON-ONLY now (no more tiny text labels under every icon).
FIX: Share-card editor — the auto-layout sparkle now truly parks the author/year info row BETWEEN the title and the quick fact, TOUCHING the title: one tap lifts the row straight back up under the headline (it used to leave it below the fact on some layouts).
FIX: Share-card editor — the favorite-tracks List / Rows toggle icons render correctly again (the old glyphs weren't in the icon font and showed as raw text names), and tapping the favorites strip now auto-opens its sizing controls (strip width, songs, List/Rows).
FIX: Share-card editor — the collage POLAROID got a full makeover: it is now movable (select it and drag its grip anywhere on the card) and its frame adapts to your photo's shape (a landscape print is wide and short, a portrait one tall). The tape now PEERS out past the white frame and sits ON TOP of the photo, so it reads as real tape sticking the print to the page.
ADD: Share-card editor — the collage polaroid has 5 frame styles (Classic · Retro · Sunglow · Vintage · Dashed) and 5 photo filters (None · Noise · Nostalgia · B&W · Warm): a new Polaroid button in the full-screen editor's top bar (Collage cards only) picks the style, filter and print size.
FIX: Share-card editor — the Signature card's favorite-tracks strip now wears the DESIGN's own ink (it used to use the tone's ink, which went near-black on bright signature scenes like Mario's red) and moved UP to sit just below the title/author instead of overlapping the quick fact at the bottom. The Collage strip also moved up under the title with stronger ink, and the sparkle's collision repair no longer shoves the strip or the fact around the wrong way on any design.
FIX: Share-card editor — the big category icon floating in the middle of the Collage card is removed (the faint corner glyphs stay).
ADD: Share-card editor — PINCH a sticker on the card to resize it (two fingers), on top of drag-to-move and the size slider.
FIX: Share-card editor — emoji STICKERS in the full-screen editor: a Stickers button in the top bar opens a picker; tap to drop a sticker on the card, drag it anywhere, resize it with a slider, and re-stack it with To front / To back. Stickers save with the card and ride onto the shared image.
FIX: Share — turning on "Include a link" now shares the card PICTURE too (it used to send only your words + the URL): the link rides as Telegram-style hidden-link text — [Topic](url) shows the topic name with the URL hidden behind it, next to the photo.
FIX: Share-card editor — the sparkle's auto-layout now keeps the AUTHOR/YEAR info row BETWEEN the title and the quick fact: a grown fact covering it lifts the row back up (and the title nudges up a little to make room), and anything dragged off the card edge is pulled back inside on the same tap instead of staying half off-screen.
FIX: Share-card editor — the move grip always wins the touch now: when two boxes overlapped, the box drawn on top used to steal the handle's drags (the "handle doesn't work" bug) — tapping or dragging the handle always moves its box.
ADD: Settings — a Book browser lists every book line by line (cover, name, author · year, cached ★ rating) in a scrollable list; the horizontal cover strip moved out of the Book covers & ratings hub into this new browser.
FIX: Book notes sheet — the chapter note box is palette-aware in dark mode now: its background, placeholder, note text and Expand chip all use the sheet's cover-tinted ink instead of the raw Material gray, so the "Add a note…" field reads cleanly on light AND dark cover palettes (no more flat gray box with dim text on the coloured wash).
CHANGE: Share — the separate Link button is gone: every Share tap opens ONE dialog where you write your message and flip an "Include a link" switch — on = your words + the topic's link share as text (Google search, or your music service for songs); off = the picture shares as before, with your caption attached (or none). The plain picture + caption path is the default.
ADD: Share-card editor — album favorites grew up: the strip hugs the longest song title instead of always filling its max width, the strip tool gains a SONGS slider (1 – all heart-picked tracks, remembered per design) and a LIST / ROWS toggle (side-by-side chips, applies to every design), and when No fact is on the strip automatically gets bigger type and shows every track in the freed space.
FIX: Share-card editor — Collage and Signature favorites are PLAIN TYPE now (no dark badge, no border): they wear the card's own ink so they read as part of the design. On Collage the strip moved out of the dark bottom band to sit above the category pill + quick fact and below the title/info; on Signature it sits clear of the quick fact and footer. The sparkle's one-tap repair covers the favorites strip too, and the smart-fit / sparkle cycle never overlaps it by default.
FIX: Share-card editor — the sparkle now repairs REAL overlaps from measured boxes instead of guessing from drag offsets: a grown quick-fact box that covers the author/year rows (or the favorites) is pushed back clear on tap — the info-row "reappear" case finally works.
CHANGE: Share-card editor — manual placement always wins now: dragging the quick-fact box no longer shoves the title or author/year rows aside, and growing the box with the sliders no longer lifts the title on its own. You can overlap elements however you like while editing; only the auto-layout sparkle repairs an overlap when you tap it.
CHANGE: Share-card editor — the auto-layout sparkle now fixes the AUTHOR/YEAR info rows too (not just title + fact): one tap moves the overlapped rows clear along with everything else. The pill also cycles SEVEN arrangements now (fit → condensed → book columns → editorial drop cap → maximal space → tall 9:16 → standard) instead of four.
ADD: Share — a new Link action opens a short message editor, then shares your words with the topic's link at the end (open the topic in Google; albums, songs and artists link to the music service you picked in Settings). The topic's name is pre-filled and editable before sharing.
CHANGE: Share-card editor — the quick-fact box can now grow WIDER than the design's default (the Fact-width slider runs to 120%): instead of leaving empty side space unused, the box expands into it — Paper and the other designs now put that space to work. Default cards are unchanged.
FIX: Share-card editor — the Signature designs no longer float the book/album cover over their category badge and title: the jacket is now built into each signature layout right beside the title (the same "glued" treatment Paper, Clean, Editorial and Minimal already use), so it sits naturally in every signature look, rides title drags / collisions / the auto lift, and never covers the badge, crest or headline.
FIX: Share-card editor — a hand-dragged title now keeps exactly the spot you park it in: the collision auto-lift stands down once you've placed the title yourself, so you can deliberately overlap it onto the quick-fact box (or anywhere else) and it no longer snaps back or bounces on release. Reset Layout returns the title to automatic collision behaviour.
CHANGE: Share-card editor — the auto-layout sparkle's 9:16 flip now makes the fact text LARGER and the box LONGER (it rides the Size/Fact-height slider channels), so switching to the tall card buys real readability instead of just empty space. The pill itself is now a crisp opaque circle with a hairline ring — the old translucent fill's shadow halo (the "solid fill glitch") is gone.
FIX: Share-card editor — the Collage design on dark premium tones (Midnight / Ember…): the light accent colour no longer floods the bottom half. Its layers now darken top → bottom on dark tones (paper → field → band → footer) so the white fact text stays readable, the torn seam stays visible, and the polaroid's handwritten caption uses a fixed dark ink (it was near-white on the white polaroid). The bottom band/footer tear is also blended with feathered gradients instead of hard wavy cuts.
ADD: Share-card editor — an AUTO-LAYOUT sparkle pill floats on the card (resting preview AND the Customise editor): one tap fits the whole card (the quick-fact box grows, then the text size adjusts through the same sliders you'd use), tap again to cycle alternative arrangements — standard fit, condensed lines, book-page columns for long facts — and if the text still overflows a 3:4 card the card flips to tall 9:16 on its own. Nothing ever overlaps and your manual positions stay.
FIX: Share-card editor — Paper's quick-fact and quote text now use ink matched to the fact pane's real blended colour: pure white on dark premium tones (Onyx / Midnight / Wine / Cocoa…) and near-black on the light paper tones — no more dark-on-dark text on any tone, in light or dark theme.
CHANGE: Share-card editor — Smart fit is now TEXT-FIRST: the quick-fact box keeps its full height and the text shrinks with the length; the box only grows when the text can't shrink any more. The Smart-fit switch clears a manually-resized box when you turn it back on (it re-fits the box), and the Whole-box slider is gone from the Crop tool (Smart fit + the sparkle pill cover it).
FIX: Share-card editor — the title no longer bounces up and down when the quick-fact box grows into it: the collision lift was computed in px but applied as dp (≈3× too far on a 3× screen, slamming the title into its clamp and bouncing). It now stores real dp, uses a dead-zone, and stands still while either box is being dragged.
CHANGE: Share-card editor — the Title-height slider only appears when the title is long enough to wrap (it caps wrapped lines), so it never reads as a dead control on short titles.
FIX: Share-card editor — Smart fit is fully transparent now: the Fact-height slider shows the grown box the card actually renders (like the text thumb already did), and Reset Layout clears the fit's auto-shrunk text so a reset card smart-fits again.
FIX: Share-card editor — the invisible leftover text-size multiplier (the old corner-zoom) is gone: one less hidden factor between the sliders and the rendered text, and old saved cards keep their exact size.
FIX: Share-card editor — Paper's quick-fact and quote text no longer inherit the app theme colour: it renders in the card's own ink, so on dark premium tones (Onyx / Noir / Midnight…) the fact stays readable in any app theme.
ADD: Share-card editor — the Bold / Italic / Underline / Highlight bar now floats over inline fact editing on the bottom-sheet preview too, not just full screen: select letters right on the card and style only that selection.
ADD: Share-card editor — full screen: Bold / Italic / Underline are now clean icon-only round toggles, Justify joins the alignment row, and the whole-element highlight swatch row is gone (highlight lives only on the floating bar over a selection).
ADD: Share-card editor — Underline everywhere: a U floats with the Bold / Italic / Highlight bar and formats only the letters you select, the whole-element format row (title + quick fact) gained its own Underline, and underlines survive on the card when you save or share the image.
ADD: Share-card editor — the Book page fact layout now has a Column gap slider (hug the two columns or spread them like a real page), in the sheet's Align tool and the full-screen Text panel.
ADD: Share-card editor — the toolbar tool names (Text · Size · Crop · Fit · Font · Color · Adjust · Align · Format · Content) now show under every icon permanently, so the toolbar reads without opening a panel.
FIX: Book Notes sheet — the accent hairline divider is gone and reading progress lives in ONE clean rail label ("2 of 4 Chapters read" — no bar, no duplicate "2 / 4"); chapter numbers, the read toggle and the note Enlarge icon use high-contrast ink on the open accent-tinted rows, and chapter row titles render capitalized.
FIX: Album track list — the ✕ close button is removed (swipe down dismisses), matching the book and series sheets' no-close model.
FIX: Share-card editor — dragging the TITLE no longer fights the box collision: the auto-lift only pushes the title when the QUICK FACT grows into it, so the title follows your finger when you move it over (or above) the fact box.
ADD: Share-card editor — full screen gains a Reset Layout button: it returns positions, box crops and auto-lifts to the design defaults while keeping every text change (fonts, sizes, formats, colours) intact.
FIX: Share-card editor — Smart fit now shows through the Text-size slider: the thumb reads the size the card actually renders (the fit's auto shrink is folded in — no more "still at 1×"), dragging stays what-you-see, and Reset puts your 1× base back.
FIX: Share-card editor — Paper no longer shrinks long fact text toward 0.8×: the fit grows the fact box instead; Collage shrinks long text more eagerly so it stays inside its band.
FIX: Share-card editor — the Auto tone now matches the topic: a book (golden category accent) Auto-lands on the Golden Ochre card instead of a dark premium-looking tone.
FIX: Share-card editor — the bottom sheet can't be half-dismissed while editing (drag gestures are off until Done/back — no jumpy spring that froze the tools); normal swipe-to-close returns the moment editing ends.
FIX: Share-card editor — the full-screen preview is no longer double-zoomed: the zoom scales density only, so text and boxes match the bottom-sheet card exactly (no more text swelling bigger than the card or cutting off at the bottom).
FIX: Share-card editor — the corner whole-box grip icon is gone (the Crop sliders own box size now), which also removes the glitchy box outline in full screen; double-tapping the fact text arms inline editing directly, no tool needed.
FIX: Share-card editor — the design options are gone from the Customise toolbar: designs switch by swiping the card, and a Style button now appears only on a Signature card where one tap flips between its two looks (Current / Classic).
ADD: Share-card editor — the dimensions button shows the ACTIVE size (3:4 / 9:16) under its icon and no longer closes the tool panel you're in; every open tool now names itself in a tiny caption under its icon, so the toolbar reads at a glance.
ADD: Share-card editor — the Standard / Condensed / Book page / Editorial fact layouts (plus the Editorial drop cap) moved out of the Bold tool into the Alignment tool, where they sit with the text alignment.
ADD: Share-card editor — "No fact" is now an eye-cross icon: an icon-only round pill in the Content list and just the eye-cross in the bottom content bar (no words) that hides the fact box.
FIX: Share-card covers — the book/album/series jacket is now GLUED to the title block on Paper, Vinyl, Clean, Editorial and Minimal: it always sits exactly beside the title wherever that design flows it (Paper beside the title+author with a snugger gap, Vinyl lower so it clears the category pill, Editorial/Minimal aligned to the headline start, Clean beside the centered title), and it rides every title move — drag the title, push it with the fact, or let the box collision lift it and the cover comes along. Album art stays a square, books/series stay 2:3.
FIX: Share-card editor — growing the quick-fact box (height / width / whole-box sliders or the corner grip) now COLLIDES smartly: the title lifts just enough to keep the box from drawing over it, and the glued cover lifts with it; lower the box again and the title settles back. The lift is saved per design like every other edit.
ADD: Book/album/series notes sheets — the cover-art palette is now INSTANT and remembered: the colors extracted from each artwork URL are cached, and albums/series remember their resolved artwork, so a reopened sheet (or one after a restart) wears the cover palette on the very first frame instead of flashing the default color and switching a second later.
FIX: Book/album/series notes sheets — selected/open rows are readable on light covers: number chips, like hearts and icons use proper contrast ink on the accent-tinted and solid-accent rows (no more icons vanishing into pale cover colors).
ADD: Share-card editor — the Enlarge writing sheet is now a rich-text editor (bold / italic / highlight, Save-your-take style) for the quick fact, custom fact and chapter review — and the formatting stays on the card when you save or share the image.
ADD: Book notes — chapter notes are rich-text too (bold / italic / highlight in the expanded editor), and "Share as Chapter review" carries the formatting onto the share card.
ADD: Share-card editor — full screen: bold / italic / highlight now float above a text SELECTION and apply only to the selected letters (like Save your take); whole-element formatting stays for the title and one-tap whole-fact styling.
FIX: Share-card editor — the full-screen text tools no longer crash (an infinite-measure bug when the tools opened) and the sliders / swipes in them drag cleanly: the tools moved from a dropdown into a compact inline panel under the top bar.
FIX: Share-card editor — the automatic card tones no longer pick level-unlocked premium tones: Auto rotation cycles only the always-available base tones, and a locked premium tone appears only when you explicitly pick it in the Tone tool.
ADD: Share-card editor — Reading progress and the QUICK FACT now stack together like progress + custom fact already did: turn on Reading progress, pick the Quick fact, and the card shows the progress bar with the quick-fact text beneath it.
ADD: Share-card editor — the Whole-box slider is now INDEPENDENT: it scales the box as one without moving the width/height sliders (each keeps its own value), so dragging width or height no longer yanks the Whole-box thumb around. Works for title, fact and the favorite-tracks strip, in the sheet and full screen.
ADD: Share-card editor — full screen gains a Dimensions button (3:4 ↔ 9:16) next to the Text pill, and a Box size section (width / height / whole box) joins the text-tools dropdown for the selected title or fact.
FIX: Share-card editor — the full-screen preview is now an exact zoom of the bottom-sheet card: text sizes, spacing and placements scale together (the old big-box render left the text tiny and re-spread the layout, so it never matched).
FIX: Share-card editor — the Full screen button now matches the Customise pill's look (no more transparent chip) and stays available while customising, so you can jump to the large editor mid-edit.
FIX: Share-card covers — book and series covers are back to the proper 2:3 jacket size and album art is no longer stretched: square album covers render square (66×66) beside the title, and the title shift now hugs each cover's real width instead of leaving a gap.
ADD: Share-card editor — a FULL-SCREEN text editor: the Full screen button (next to Customise) renders the card LARGE on the category-tint wash with floating tools only (Close left, one Text pill right) — the pill opens a dropdown with every text tool in one place: font (13), size slider, bold / italic / underline / highlight swatches, alignment and the fact format + drop-cap pickers. Inline editing is precise because the card is big, and it uses the exact same TopicShareCard so the export matches the full-screen preview.
ADD: Share-card editor — the fact box's corner drag is now a true whole-box ZOOM: it scales the box AND the fact text together (photo-zoom style, 0.5–4×), so the corner never feels like a plain width/height slider. The zoom is saved per design and Reset clears it.
ADD: Share-card editor — whole-element rich-text-lite for title AND fact: Underline (U) and a Highlight swatch row (5 colors) join Bold / Italic in the text tools, applied per element and saved with the design.
FIX: Share-card editor — while typing the quick/custom fact the selection chrome hides: no outline or highlight around the fact box (the caret shows where you type), and the move grip + corner handle disappear too; they all return the moment text editing ends.
FIX: Share-card editor — the smart auto-adjuster now visibly adjusts the quick/custom fact: long facts shrink their TEXT size (per style, from ~150 characters up) instead of only getting a taller box, and medium facts (90+ characters) start getting extra room earlier than before. Grabbing the fact to move it keeps the adjusted size (no pop back to full text size), and Reset restores everything.
ADD: Book notes — chapter notes are no longer stuck in a tiny single line: an EXPAND button opens a full white writing sheet for long notes, and a SHARE button on any non-empty note opens the topic share card pre-seeded with that note as a Chapter review (tagged with its chapter) — no re-typing.
ADD: Share-card editor — album and series topics now get the same cover-fetch flow as books: Fetch / Refetch / Remove in the Content panel resolves the artwork (iTunes → MusicBrainz for albums, TVMaze → iTunes for series), and the cover renders LEFT of the title like the synopsis page (title shifts + wraps around it, auto-shrinks when the cover is on). The favorite-tracks strip's box tool gained a Whole-box slider (width + rows together).
FIX: Book/album/series notes sheets — the cover colors now come from the artwork's TRUE dominant colour (a direct pixel-vote histogram replaces the unreliable androidx Palette guess), and albums/series finally feed the RESOLVED artwork URL into the palette instead of their empty authored imageUrl — so the sheets wear the colours you actually see on the cover. The expanded chapter/episode row is a soft accent tint with a border now instead of a loud solid slab.
FIX: Share-card editor — dragging the quick fact box no longer drags the title and author row along by default: they only move now when the fact actually RUNS INTO them (collision-push), so moving the fact away leaves the title/author put, and pushing up into them moves them cleanly out of the way.
FIX: Book covers — Google Books is removed as a cover source (its keyless search rarely returned a usable thumbnail); books now fall back to iTunes search (title+author) then Open Library everywhere, in the share card, the reveal poster and the Settings hub. ★ ratings and ISBN lookups still run keyless Google Books in the background.
FIX: Share-card editor — the smart auto-fit now respects each style's own layout instead of blindly shoving things: on Collage the long quick fact no longer slides up over the category pill and the title no longer leaves the card, on Editorial the headline no longer rises over the masthead rules and the fact no longer overlaps the author/year row, and on Clean/Minimal the title lifts a little so the growing fact clears it. A long title now shrinks to fit its slot when the fact needs the room.
FIX: Share-card editor — long quick AND custom facts now auto-fit (the smart adjuster was only watching the quick fact), the Collage/Clean/Editorial/Minimal long-text floors are raised so the text stays readable as the box expands, and the fact box height/whole-box sliders reach 6x to match the corner grip's expansion.
FIX: Share-card editor — the whole-box corner grip now works on the info row and the favorite-tracks strip too (title and fact already had it): select any box and drag its bottom-right corner to scale width AND height together, photo-editor style.
FIX: Book/album/series notes sheets — the cover-extracted colors now actually match the artwork: the sheet wash follows the cover's real hue instead of washing out neutral (the old recipe keyed off the near-grey dark swatch), black-and-white/minimal covers fall back to the category tint instead of a grey sheet with an invisible black accent, and the accent glow under the drag handle now changes color with the sheet instead of staying the category color.
FIX: Book synopses — 32 more books now read as proper, connected synopses instead of choppy topic lists (the non-fiction ones used to rattle off "he shows this, he explores that"): Predictably Irrational, Nudge, Ego Is the Enemy, Stillness Is the Key, The Tipping Point, Algorithms to Live By, The 48 Laws of Power, The Dispossessed, The Princess Bride, The Jungle, Sister Carrie, Uncle Tom's Cabin, Love in the Time of Cholera, Gideon the Ninth, The Souls of Black Folk, The Tale of Genji, The Four Agreements, 12 Rules for Life, The Dark Forest, Solaris, The Last Unicorn, The Power of Now, The Prophet, Homo Deus, The Art of War, Outliers, Grit, The Midnight Library, Quiet and more.
ADD: Book synopses — 60 books now have proper, detailed web-researched synopses, replacing thin one-line blurbs (latest 30: Up from Slavery, Influence, Little House on the Prairie, Freakonomics, The Ocean at the End of the Lane, My Antonia, The Awakening, The 48 Laws of Power, Watership Down, The Republic, The Tombs of Atuan, Lessons in Chemistry, Ethan Frome, The Tipping Point, Pedro Paramo, Tom Sawyer, Assassin's Apprentice, All Systems Red, Sapiens, Of Mice and Men, Rendezvous with Rama and more).
ADD: Series notes — synopsis + episode lists also added for The Good Place, Ted Lasso, The Mandalorian, Succession and True Detective (20 shows now carry episode data).
ADD: Book synopses — 30 books got proper, detailed synopses (web-researched), replacing thin one-line blurbs: Nudge, Predictably Irrational, Meditations, A Suitable Boy, The Graveyard Book, Small Gods, Wild, Our Town, The Invention of Morel, Tarzan of the Apes, The Elegant Universe and more.
ADD: Series notes — synopsis + episode lists added for Breaking Bad, Stranger Things, Game of Thrones, The Wire and The Sopranos, so the reveal's series card appears for those shows too.
FIX: Album links — the LISTEN / Listen-in Apple Music and Spotify links no longer open the wrong album: Apple Music now resolves the album through the artist's own catalog (the search API ranked tributes and same-title singles above the real album — Nevermind and Dark Side of the Moon never even appeared in the top 25 hits), and anything that can't be confidently matched now falls back to a plain search instead of a wrong or blank page. Spotify's lookup also got stricter matching.
FIX: Book covers — "Fetch all covers" now uses ONLY the provider you picked (no more silent fallback to the old authored covers, so switching providers after a Clear actually shows that provider's artwork), and "Clear all covers" now also clears the in-memory image cache so the strip visibly empties instead of showing the old covers.
ADD: Book notes — each chapter now has a personal note field in its expanded panel (a quiet "Add a note…" line that saves as you type, blank clears it), so you can jot thoughts next to any chapter and they survive restarts.
ADD: Book covers — a "Clear all covers" button in the book-covers hub (with a confirm prompt) wipes every stored/verified cover and the downloaded images so the next Fetch starts fresh, making it easy to compare cover providers. Ratings are kept.
FIX: Book covers — the reveal poster no longer gets stuck on a 1x1 placeholder cover (the tiny-image skip now works on the current Coil API), and when a LibraryThing key is configured the hub now picks it as the default cover source on a fresh install.
ADD: Series notes — synopsis + full episode lists added for Sherlock, Squid Game, The Last of Us, Severance and Wednesday, so the reveal's series card (poster + synopsis preview + episode list sheet) now appears for those shows too.
FIX: Book covers — the bulk fetch now VERIFIES a cover actually decodes to a real image before counting it: Open Library serves a 1x1 placeholder for missing covers, so books like A Handful of Dust and A Perfect Spy (dead authored URLs) now fall through to the real provider cascade (iTunes → Google Books → Open Library → LibraryThing) instead of silently "succeeding" with a blank tile.
FIX: Book covers — "Fetch all covers" now SKIPS books whose covers already verified, so re-tapping resumes where the last run stopped instead of restarting from book #1; and the fetch keeps running when you leave the hub page (re-entering shows live progress, Cancel still works).
FIX: Share-card editor — the author/year info row can now be dragged to the card's true edges like every other element (it used to be stuck ~18px inside, so it couldn't reach the sides).
FIX: Album share card — the accent rule above the quick/custom fact on the Minimal style now MOVES with the fact box (it used to drift away when you dragged); the favorite-tracks strip on Collage is now a translucent dark slip (it clashed with the collage's dark band) and on Signature/Custom a dark stamp pill that reads on any background, and both are placed higher so they never overlap the footer.
ADD: Spotify — with free developer credentials (SPOTIFY_CLIENT_ID + SPOTIFY_CLIENT_SECRET, see .env.example) the album sheet's LISTEN pill and the Explore "Listen in" action now open the REAL album, track or artist on Spotify (client-credentials lookup → open.spotify.com deep link) instead of a search; unset keeps the search links.
FIX: Album track-list sheet — the LISTEN pill's Apple Music entry now opens the REAL album (an iTunes lookup resolves the native album page, the same deep link songs use) instead of a search; it only falls back to searching when the lookup misses. The other services (Spotify, YouTube Music, Amazon Music, Deezer) have no keyless album-ID lookup, so they keep the search link.
ADD: Book covers — iTunes is now the FIRST cover source: the keyless ebook search is tried before Google Books and Open Library everywhere (the reveal poster's live fallback and the Settings hub's default), and its artwork is upscaled for the poster.
ADD: Book covers — a LibraryThing cover source is now available in the Settings hub (free key via LIBRARY_THING_API_KEY, see .env.example): it resolves each book's ISBN through a keyless Google Books search, then fetches the high-quality cover; without a key the row stays hidden and the keyless providers do the work.
FIX: Book + series notes sheets — the ✕ close button is gone; swipe down or back dismisses like every other sheet.
FIX: Book notes sheet — the Reviews & Your-rating card is removed: the rating now sits just below the author name (the Google Books average and your own pick together) under one award-ribbon icon instead of a star.
FIX: Book + series notes sheets — a read/watched chapter or episode no longer flashes a loud ✓ disc: the leading chip keeps the number and just tints softly in the accent, and the mark-read toggle keeps a solid accent fill in every state (no more dark-mode punch-hole look on expanded read rows).
FIX: Book + series notes sheets — the ▼/▲ expand arrow is removed from chapter/episode rows and the "tap a chapter to read its notes" hint is gone; rows still expand on tap.
ADD: Book covers — the reveal poster now LIVE-resolves a Google Books thumbnail the moment every static cover candidate fails (authored URL, hub-resolved URL and the Open Library guess all 404) and remembers it, so a cover can appear without visiting the Settings hub; only runs while book-cover fetching is ON.
ADD: Book covers — optional GOOGLE_BOOKS_API_KEY (free Google Books API tier): set it as an env var / GitHub Actions secret (see .env.example) and every keyless Google Books call upgrades to the keyed higher-quota endpoint automatically; unset keeps the fully keyless path.
ADD: Book notes sheet — the "Your rating" picker swapped the fountain-pen nibs for five open-book glyphs, and it is now its own hideable element (like reading progress but separate): the book icon in the row header hides/shows the picker without ever clearing your rating.
FIX: Book + series notes sheets — read/watched chips now render as SOLID fills with a clean rim (no more washed-out discs), the row surface keeps a flat shadow (the 1dp lift used to pop in/out on expand), and expand/collapse animates height + fade together instead of the glitchy fade-only pop.
FIX: Share-card editor — the magnet SNAP is removed entirely (boxes followed your finger but kept sticking to guides, still too strong): dragging is now completely free with a faint full-card alignment hint while a box lines up with the card or another element, and the box never grabs.
ADD: Album share card — the favorite-tracks strip is now a MOVABLE element like the book cover: select it in edit mode and drag the grip anywhere on the card; its position saves per style with the other edits.
ADD: Album share card — the heart-picked favorite-tracks strip is no longer the same sticker on every design: Paper/Vinyl/Collage keep a (restyled) badge, Neumorphic a dark pill, Signature/Custom a tone pill, while Editorial renders it as masthead type (rule + caps label + italic tracks) and Minimal as a quiet single line — no box on the boxless cards.
ADD: Album share card — the favorite-tracks strip's icons are now per-style: a filled music note (Paper, Collage, Signature), a tiny vinyl record disc (Vinyl), equalizer bars (Neumorphic), a masthead star + note ornaments (Editorial) and a quiet dot (Minimal) instead of the same heart everywhere.
FIX: Album share card — the favorite-tracks strip's position is fixed per style: Minimal moved off the top-end (where its glyph + the cover jacket sit) to bottom-start, Editorial cleared the colophon slug, and the rest nudge clear of footers.
ADD: Album share card — the share editor now has a Show / Hide toggle for the favorite-tracks strip (heart pill next to the "Favorite tracks…" note, on any style, no picks lost) so the user can drop the strip from the card whenever they want; hiding it also brings back the typed favorite-song line on Vinyl.
FIX: Share-card editor — Save/Share now persists what content you picked and wrote, not just the size/position edits: the custom-fact text, the chapter-review text and its chapter chip, the reading-progress bar toggle and the collage caption all restore when the sheet reopens for the same topic (restored picks are re-validated against what the topic actually offers).
FIX: Book + series notes sheets — chapter/episode switching no longer lags: chip taps snap INSTANTLY to the chapter (the old animated glide over long lists is gone) and tapping a row no longer round-trips through the reveal's selection state, so the sheet no longer resets or re-scrolls on every tap.
FIX: Book notes sheet — the synopsis now starts COLLAPSED (was pre-expanded): the About-this-book accordion shows a two-line teaser and you tap Read to open the full text; the chapter you opened from still expands + scrolls into view.
FIX: Topic Reveal — chapters without a summary no longer render as empty boxes on the reveal chip row; they stay in the sheet's full chapter list (still readable there), only the empty preview chips are hidden.
ADD: Book notes sheet — the Mark-read toggle and a per-chapter Like heart now live as chips ON every chapter row (not hidden inside the expanded panel): read/watched chapters get a SOLID accent chip (open-book icon for read) and liked chapters a red heart; the expanded panel now only holds pages + the chapter notes.
ADD: Book notes sheet — a compact Reviews card under the progress rail shows the fetched Google Books rating (small star + ratings count) next to YOUR RATING, a new fountain-pen-NIB rating picker (5 ink nibs instead of stars, tap again to clear) stored per book.
FIX: Book ratings — the keyless Google Books fetch used to ABORT on the first search hit without a rating, so most books never got one; it now keeps scanning later hits, so "Fetch ratings" in the book hub finds many more stars (and the reveal chip shows the ratings count too).
FIX: Book covers — covers the hub resolved via Google Books never appeared on the reveal (the reveal only knew the authored URL + the Open Library fallback); the resolved URL is now persisted per book and used by the reveal poster, the sheet's palette and the share card, so hub-fetched covers show up everywhere.
ADD: Series episode sheet — episodes mirror the book sheet: per-row Watched chip (solid accent when watched, open-book icon) + episode Like heart on the row instead of inside the expanded panel.
FIX: Series reveal — SERIES topics now appear on the reveal page: a poster card (authored imageUrl first, then the keyless TVMaze + iTunes fallback only when series fetching is on) with synopsis preview + episode count, opening a full album-style episode-list sheet.
ADD: Series episode-list sheet — album-style one scroll (mirrors book / album sheets): poster header + favorite heart, pinned watched-progress rail, the "About this series" synopsis accordion at the top, then episodes grouped by season as expandable rows with summary + the Watched / Undo pill; favorite + watched data persist per show.
FIX: Cover fetching — books, albums and series each now have their own Settings toggle (all OFF by default like the book toggle), so poster fetching can be switched on per category; the reveal gates the keyless lookup on its own toggle rather than always fetching. Album's existing iTunes / MusicBrainz art fallback now also respects its toggle.
ADD: Series episode data — series topics now carry real synopsis + episode data (season, number, title, one-line summary) modeled on the album track schema; first batch web-researched and verified (Chernobyl, Band of Brothers, The Queen's Gambit, Watchmen, Fleabag, Freaks and Geeks).
ADD: Book notes sheet — redesigned album-style into one scroll: an About-this-book synopsis accordion pinned at the top (Read/Hide) with the full chapter list as expandable rows below, replacing the Synopsis | Chapters tabs; expanding a chapter shows its pages + notes inline with the Mark-read / Undo pill, and the reading-progress rail stays pinned above the list.
ADD: Books — a favorite heart now sits in the book-notes sheet header (next to close), toggling a whole-book favorite stored by title alongside reading progress and fetched ratings.
FIX: Topic Browser cold-open load — the catalog used to be derived TWICE (a per-category fallback, then a swap to the merged-index source once it landed), rebuilding all ~20k search entries + rows on every swap; it now derives ONCE from the loader cache, and the Room mirror (delete + full-lane insert of chapter/track-heavy entities) no longer blocks the first paint — rows appear as soon as the JSON parses and Room catches up in the background.
FIX: Topic Browser category switching — flipping lanes no longer re-maps and re-sorts the whole catalog: browse rows come from the pre-grouped lane lists, so a switch only walks the selected lanes' topics and the new list appears right away (was a visible beat on big lanes).
FIX: Topic Browser category panel — ticking categories then tapping Done now actually applies the pick (the old code committed the CURRENT selection instead of the pending one, so the filter silently never changed).
FIX: Spin — the shuffle button is trimmed ~10% (102/92dp idle, 88/82dp landed; the orbit ring follows) so the CTA reads slimmer next to the deck instead of an oversized disc — idle and while spinning.
ADD: Share-card editor — while you drag any box (title, fact, info row, chip, book cover) PicsArt-style alignment guides appear: a faint center crosshair shows the moment you pull a grip, and edges/center magnet-snap to the card's own center lines and edges with a bright full-card guide line marking the snap.
ADD: Share-card editor — text size is now a precise 0.5x–2.0x slider in 1% steps with a haptic tick per step and a live x readout (replaces the coarse buttons that jumped straight from too-small to cut-off for long titles/facts), with a one-tap Reset to 1x.
FIX: Share-card editor — every box can now be dragged to the card's true edges (Clean title that "wouldn't go high", fact/meta stuck partway): clamps are measured from the unmoved position each grab, so the range never shrinks as the box travels.
ADD: Album share cards — the heart-picked FAVORITE TRACKS strip gets its own look per card style: Vinyl keeps its cream chip, Paper/Editorial/Collage wear parchment + serif italics, Minimal turns into a square-cornered sans slug, Clean (Neumorphic) into a rounded dark pill with white type, and Signature/Custom reuse the tone palette — each in the corner its design leaves free; on Vinyl the typed favorite-song chip steps aside so the two never overlap.
FIX: Book share cards — the cover now defaults to the pocket each design leaves free (Paper/Vinyl top-right, Clean below its big glyph, Editorial bottom-right beside the colophon, Signature below the crest) instead of always sitting on the same corner art.
FIX: Topic Browser performance — search typing and the loading/search toggles no longer rebuild the whole ~16k-topic rows pipeline on the main thread per recomposition: the warm seed is remembered per index instance and the index remap runs off-thread, so typing, opening the category panel and first load stay smooth.
ADD: Album share cards — the songs you heart-picked in the album's track list show on EVERY card style (Paper, Vinyl, Collage, Neumorphic, Editorial, Minimal, Signature, Custom) as a FAVORITE TRACKS strip (three songs + "+N more"), replacing the typed favorite line.
ADD: Category picker — Continue exploring splits into an auto top row (your most-spun lanes, driven by your spins alone) and your hand-picked rows below (remove or add more); holding any lane there starts a live multi-select mix applied by a Mix · N capsule, and hold-to-mix now works on the Curio and Knowledge tabs of the classic picker too.
FIX: Category picker — tap-and-hold option menus no longer pop open while you scroll: any scroll-drag past touch slop cancels the hold before it fires.
ADD: Book and album notes sheets now wear a FULL palette extracted from the cover artwork (the vibrant swatch pops as the accent, dark shades anchor the background, lighter shades lift the cards and chips, and text tones adapt to the wash) so every element reads distinctly; when there's no cover they fall back to the category tint.
FIX: Book reading progress — the chapter chip and header now show the BOOK's own chapter label from its title ("Chapter 57", "Letter I", "Part II", "Epilogue"…), so offset/letter books (Frankenstein, The Graveyard Book, Moby-Dick) no longer read "CH 5 · Chapter 1"; grouped-range entries were split into individual chapters (The Divine Comedy is now its 100 cantos, Dead Souls 15, Lady Susan 41 letters + conclusion, The 48 Laws 48, War and Peace 361 chapters…), Heaven now lists its real 9 unnumbered chapters, Moby-Dick's numbering matches its titles, and every book's chapter numbers are sequential — so progress counts real chapters.
FIX: Book chapter data repair — 20 classics with wrong counts now list every real chapter (Jane Eyre 38, Great Expectations 59, Don Quixote 126, The Color Purple 90 letters…), and the split `chapterNumber`/`number` chapter schema is unified so progress behaves the same for every book.
FIX: Book cover placeholder — a missing or still-loading cover shows a proper rounded book-plate placeholder instead of an empty see-through box, so the shadow under the poster reads clean.
ADD: Albums on Topic Reveal — the track-list sheet opens with the album synopsis in a collapsible About-this-album card at the top (Read expands, Hide collapses), every song row has a heart you can tap to multi-pick favorites, and a LISTEN pill opens the album straight in Apple Music / Spotify / YouTube Music / Amazon Music / Deezer (no explore session).
FIX: Topic Browser performance — the whole ~16k-topic rows pipeline (catalog grouping, search index, row build) was being rebuilt over and over on every recomposition, churning tens of MB and forcing a GC every second or two; the lane list is now computed once per filter/search change, so browsing and scrolling stay smooth and the list stops re-loading by itself.
FIX: Share hub crash — opening the hub on release builds could crash with a verifier error (R8 was merging the share-card composables into one giant method that the device refused to load); the share-card and hub files now skip that optimization.
FIX: Topic Database cleanup — topics removed, renamed or deduped in a data update now actually LEAVE the device: the Room catalog sync mirrors the shipped data exactly (delete + re-import per lane) and also runs on every install, not only when the version number changes, so old duplicate listings stop lingering in the browser forever.
FIX: Topic Database category panel — checkboxes stage your pick and the list updates ONCE when you tap Done (no more re-filtering + lag behind the panel on every tap), and switching category or settling a search no longer flashes "No topics match" while the new list builds — the previous rows stay put until the new ones are ready.
FIX: Spin deck loading — the deck now fills instantly from a small random sample of your lane while the full topic pool loads in the background; once the full pool is ready the deck re-deals and spins draw from the whole catalog, never the sample.
FIX: Tablet — the experimental "editorial no-tear" tablet redesign is reverted: tablets and landscape windows go back to the mobile design (Topic Browser returns to a single-column list where tapping a row opens the topic, Spin keeps its category + filter pills under the deck, and the torn heroes and Home stat row return everywhere).
FIX: Tablet landscape — torn page headers are no longer stuck over the content: on wide windows they now scroll away with the list (Topic Browser, Cabinet, Recents, Settings and every torn-header page), so a landscape tablet keeps the full screen for browsing.
FIX: Share-card crash when exporting a card with a book cover — the cover was decoded as a hardware bitmap, which a software-rendered PNG export can't draw ("Software rendering doesn't support hardware bitmaps"); covers now decode software-safe so Save/Share works with any amount of text.
FIX: Topic Browser search — results come back instantly and are split into two labelled groups, "Exact matches" first and typo-tolerant "Similar matches" below (both searches shown); whoever has the matching TITLE ranks first in each group, so a title hit beats a description-only hit. The catalog is scanned ONCE per query (was twice) and topic words are pre-split at load instead of re-split on every keystroke.
FIX: "Also in" pills — lanes that own a topic whose TITLE matches the query now rank first (then by hit count), so the category you're actually searching for shows up instead of being buried under high-count fuzzy lanes.
FIX: Book covers on Topic Reveal — covers and ★ ratings no longer auto-download when the book-fetch toggle is OFF (they hit the network even with consent never given); with fetching off the poster only serves already-cached covers.
FIX: Performance — scrolling a very long saved note (or any note-paper detail page) no longer churns memory: the note's rich text is now built once instead of on every redraw, the page skips its glass-backdrop capture when glass is off, and each paper card's texture and coffee stains are prebuilt once per size instead of re-rolled on every frame.
ADD: Share-card chapter progress + custom fact — Reading progress stays its own separate bar and you can stack your custom fact UNDER it, on every card style (Paper, Vinyl, Collage, Neumorphic, Editorial, Minimal, Signature, Custom) — not just Paper like the earlier build.
FIX: Share-card font sizing — shrinking or growing the fact text no longer shrinks or grows the fact BOX with it: the line count compensates so the box keeps its footprint, and a long paragraph finally fits when you drop the font size.
FIX: Share-card book covers — the jacket no longer auto-appears on the card: tap Fetch in the editor's Content panel to load it (Try again after a miss, Refetch for a different cover), and once on the card you can DRAG the cover to any spot like the category chip.
FIX: Cabinet tab icon — the Cabinet shelf icon renders again (the bundled icon font had dropped the glyph the tab uses, so it showed its raw text name).
FIX: Share-card Adjust tool — raising Contrast now makes the card DEEPER instead of washing it white: the contrast pivot sits near the top of the range for light parchment cards, so the cream fields slide down to a richer tone and the ink deepens instead of the whole card blowing out.
FIX: Collage share card — the picked Tone (including the level-unlocked tones) no longer floods the lower field in raw saturated colour: the field, dark band and category pill are now muted pastels of the tone (same hue and lightness, gentler saturation), so Ember, Rose Gold, Ocean and friends read soft instead of garish.
FIX: Share-card book chapters — the reveal's Mark-read button now TOGGLES (tap again to undo, progress moves both ways), and the editor's Reading progress is a slider instead of chip taps, so progress can be set and corrected exactly (drag back to undo).
FIX: Share-card custom fact — tapping the quick fact auto-converts it into a custom fact you can edit immediately; the custom-fact box no longer shows duplicate text, and clearing it keeps the box (placeholder returns) instead of it vanishing.
FIX: Share-card info row — the author/artist/year line now wraps to a second line instead of clipping when the font runs long, and the box tool gains Info width + Info lines sliders.
FIX: Book star ratings — the share card now shows the fetched ★ rating (was fetched but never rendered), and a bulk ratings fetch skips books that already have cached stars so it resumes instead of restarting.
ADD: Book share cards — the book cover now auto-appears on the card (a small jacket badge on every style, the polaroid slot on Collage) from the topic's authored cover; in the editor you can Change it from the gallery, Refetch from the keyless providers, or Remove it.
ADD: Albums on Topic Reveal — a TRACKLIST card with the album cover, artist and track preview (with total runtime) sits under the hero; tap it for the full track list in one tall sheet, or jump straight to a track via the track chips. Album artwork is fetched on the fly (iTunes first, MusicBrainz + Cover Art Archive fallback) with no API key, and cached on device.
ADD: Album covers + Genius links — the album catalog now ships a real cover URL for 929/1000 albums (shown instantly, no lookup) and a Genius album page link for all 1,000; the track-list sheet shows a GENIUS pill that opens the album's annotated lyrics page.
FIX: Album track lists — multi-part tracks the CD indexes separately now show as separate songs instead of one merged entry: Sonic Youth's Daydream Nation trilogy splits into The Wonder / Hyperstation / Eliminator Jr., A Love Supreme into Pursuance + Psalm, and Weezer's Futurescope Trilogy into its three parts; ~60 more tracks had broken quoting that jammed titles together ("Song A" / "Song B", medley parts glued with asterisks) — medleys and suites now keep their real one-line titles, and Art Blakey's Moanin' no longer lists the Drum Thunder Suite twice.
ADD: Book Notes + album track-list sheets — the sheets now wear a category-tinted wash that matches the reveal page, with a soft accent hairline under the handle, instead of a neutral floating panel.
FIX: Category picker hold menu — action labels are now short verbs (Pin, Spin, Remove, Edit, Delete) instead of long phrases like "Spin Artificial Intelligence" or "Remove from Continue exploring", so the menu card stays compact and its text never truncates.
FIX: Share-card editor — while editing, the bottom bar now shows one content toggle that says what the card is currently showing (Quick fact, No fact, a saved source…) and OPENS the content options in the same panel as before; the floating Customise pill no longer hovers over the card mid-edit.
FIX: App-wide copy polish — em dashes across hints, descriptions, card credits and empty states are rephrased into clean, elegant English (middot separators, complete sentences): quote credits and card bylines read "Author · …", the watermark is now "via Curio · Stay curious", and share-card tool headings match the app's own wording.
FIX: Performance — the baseline profile rules now use the valid HRF syntax (flags belong on method rules only), so the profile compiles into the release build as intended.
ADD: Performance — Curio now ships a baseline profile: the hot startup and UI paths are pre-compiled at install instead of JIT-compiling giant composables at runtime (first-run memory/CPU spikes are gone, and cold start is faster).
FIX: Performance — the pre-Android-12 liquid-glass snapshot engine now coalesces: it reads and blurs the page backdrop ~5×/s at a smaller resolution instead of ~8×/s at a larger one, so old devices churn ~60% less memory per second behind the glass.
FIX: Your-mixes cards — the mix identity colors are now a wider, muted premium palette (16 tones, was 10): the loud saturated reds/magentas are gone, so red-ish mixes read softly on the cream cards in light mode instead of screaming, and dark mode lifts them to gentle pastels.
ADD: Category picker — tap lanes on the classic page to start a mix, then hit the + button: your pending lanes are pre-ticked in the new-mix editor, so the selection flows straight into saving it as a named mix.
ADD: Share-card editor — layout edits are now saved PER DESIGN: each style (Paper, Vinyl, Collage…) keeps its own position and box-size tweaks, so moving the title on one card never shoves the others; you can swipe between designs while editing, and tapping empty space on the card deselects the current box.
FIX: Share-card editor — while editing, the bottom bar now holds the content selector (Quick fact / No fact / + Custom fact) plus Reset and Done right where Save/Share/Text sit, and tapping Done brings the save/share actions straight back; the move grip also hides while you type the quick fact so it never covers your text.
FIX: Category picker hold actions rebuilt — holding a tile now pops a compact icon+label menu above your thumb that STAYS OPEN after you lift (the oversized gooey ring that collapsed instantly is gone); tap an option to run it, tap outside to close, and it auto-dismisses after a few idle seconds.
FIX: Books catalog — removed 90 duplicate book entries (955 to 865 titles), every chapter is now its own entry with real page ranges (Moby-Dick 135 chapters, the Tao Te Ching 81, Journey to the West 100, the Iliad and Odyssey 24 books each), and book page numbers show on the reveal again.
ADD: Book share cards — the Reading progress content is now a VISUAL progress widget on the card (book icon + "3 of 12 chapters" over a filled bar) instead of plain text, rendered in every card style's own surface tones; it updates live from your Book Notes reading progress and the chapter chips in the editor, and the text-edit tool steps aside while it's shown.
ADD: Browse Topics search is instant — results are debounced (the full-catalog scan no longer re-runs on every keystroke) and capped at the best 50 topics, so a broad query returns immediately.
FIX: Category panel (Cabinet + Topic Browser) — checked rows now wear a visible category-tinted fill (selection was only the tiny checkbox) and the panel surface follows the theme instead of a fixed tan block.
FIX: Mix cards in the picker — cover plates and lane chips are now SOLID identity tones with contrasting glyphs (deep tone + white in light, lifted tone + near-black in dark), so mixes read with real contrast in both themes instead of translucent washes.
ADD: Book covers hub — an "All covers" strip shows every book's cached cover with its ★ rating + ratings count; the count is cached beside the average and now shows on Topic Reveal too ("★ 4.2 · 1.2k").
ADD: Book Notes — reading progress: mark any chapter read (only moves forward) with a "X of N chapters read" rail and progress bar in the sheet; chapters you've read tick off in the chip row.
ADD: Book share cards — two new content options in the editor: Reading progress ("I'm N of M chapters in", also writes your Book Notes progress) and Chapter review (your written review tagged with a chapter chip); pick the chapter from a chip row.
FIX: Book covers on Topic Reveal — restored to the last known-good render (the cover image itself is the poster; no gradient wrapper), so the real cover shows again instead of a gradient.
ADD: Books on Topic Reveal — the star rating now has a visible view: an on-demand ★ chip beside the author pill fetches and shows the book's average rating (the rating fetch existed with no way to see it).
FIX: Pet outfit shop — tapping an EQUIPPED outfit now removes it (accessories could only be put on, never taken off); the pill toggles On → tap to remove.
FIX: Share-card editor — the Edit-text circle (when the quick fact is selected) and the Reset circle now float right next to the Done button; they no longer hide mid-scroll in the tool row.
ADD: Share-card editor — the Collage card now wears the picked Tone palette (paper, field, band and ink follow the tone), so color customisation works on Collage too.
FIX: Category picker tap-and-hold — the radial menu discs are smaller (no more giant balls), they stay open a beat after you release and fade out instead of vanishing instantly, and the gooey blobs open in the correct circular ring (they were landing offset from the discs).
ADD: Signature card redesign, category by category — starting with Books: a classic cloth hardcover look (oxblood leather + gold foil double border), a real open-book icon crest, a faint open-book watermark, and gold ruled lines behind the quick fact so the text sits on the lines like a printed page.
FIX: Share-card editor — back now cancels the editor first (press again to exit the sheet), and exiting the sheet by mistake no longer loses your work: edits are saved on dismiss and restored when you reopen the sheet (they clear when you leave the topic).
FIX: Share-card editor — the Edit text circle now sits right next to the floating Done button (it used to hide mid-scroll), and the move knob shrinks and fades while dragging so it no longer covers the text you're moving.
FIX: Category picker tap-and-hold — rebuilt on the current Compose APIs so the gooey radial menu works on every device (was failing to compile on some builds).
REMOVE: The Deepen signature-card option is gone — the detailed background-scene experiment and its Settings toggle are removed; signature cards keep the default and classic looks only.
FIX: Category picker — during mix, the floating Apply pill is back beside Cancel in the page, and the big solid "Mix · N" strip behind the Cancel button is gone (the bottom capsule stays for the new page and idle state).
ADD: Share-card editor — new Adjust tool with Saturation and Contrast sliders that fine-tune the whole card live (every style, baked into the saved image); it replaces the heavy Deepen scenes with a clean edit.
ADD: Category picker tap-and-hold is now a fluid radial menu — circular actions well up out of the press point as gooey water blobs (no dark overlay), settle into a ring around your thumb, and stay live-switchable: drag anywhere and the nearest action highlights; release over one to pick it, release over nothing to cancel. Works on the Pinned pills, mix cards, Continue-exploring lanes, and the Browse/Mixes/Pins page.
FIX: The "Try a new lane" daily now pins its lane for the whole day — exploring the lane it names always completes it (other lane-spins can no longer silently re-aim it elsewhere).
REMOVE: Home — the daily-quest strip on the Home screen is gone; quests live on the Quests screen only.
FIX: Quests — the "Bonus quests unlocked!" line hides again once both bonus quests are claimed.
ADD: Share-card editor — new Tone tool with a swatch row of every tone you've UNLOCKED (level rewards now visibly buy card looks): pick Midnight, Forest, Lavender, Ember or the six new Ocean, Rose Gold, Moss, Storm, Pearl, Sunburst tones, or keep Auto rotation.
ADD: Pet shop — four new outfits (Polka Bowtie, Sun Hat, Curio Glasses, Tail Puff) plus a new Toys & games section: buy Ball fetch, Star catch or Bubble storm with sparkles, then hit Play to run a real pet play moment.
FIX: Share card editor — tool panels stay open while you switch options; tap the tool icon again to close. Picking a design, size, font, alignment or format no longer collapses the panel.
FIX: Share card editor — tapping the quick fact selects it for moving (grip appears, no keyboard); type via the new "Edit text" tool, and tapping the title, info row or category chip ends text editing and closes the keyboard.
FIX: Share card editor — the sheet can no longer be dismissed by mistake while editing (swipe/back ignored until Done).
FIX: Book covers — cover images render again on Topic Reveal and in the book-notes sheet (the gradient placeholder was painting over them).
ADD: Share-card editor rebuilt — an icon-only circular toolbar (design, aspect, text size, box size, font, alignment, bold/italic, content) with one small panel per tool; the Customise button now opens the editor itself. Tap any element on the card to select it and edit it — and the font/format tools now work on the title, quick fact, info row and category chip alike. New fonts (13 total), more alignments (incl. justify), and 3:4 is the default aspect.
FIX: Category picker polish — one mix button instead of two, compact mix cards without the text lane-list (icons say it all), hold-option pills now pop in right where you pressed, Browse/Mixes/Pins tabs each get their own color with a solid active state, and Back from Browse re-opens the picker.
FIX: Paper share card — dragging the category chip no longer drags the bulb icon along (bulb stays put), and the quick-fact width is editable again.
FIX: Books — 163 catalog entries were missing their subtype badge (blank chip on the reveal card); each now carries its real genre (Nonfiction, Novel, Science Fiction, Fantasy, Philosophy, Memoir…).
ADD: Levels now unlock things — new share-card tones (Midnight, Forest, Lavender, Ember), pet outfits, and custom lane order (at Level 5). Each level-up shows what you just unlocked, and the level card previews the next reward.
ADD: A pet outfit shop — earn sparkles from daily/weekly quests and streak milestones, then spend them on cosmetic outfits for Curie (from Quests or Profile).
ADD: Daily quests now match the real loop — "Reveal 2 new topics", "Save a discovery", "Capture with voice once" — with a live quest-progress strip on Home.
ADD: The pet celebrates on Home — a nudge bubble pops when you finish a quest; play remains available through the pet itself.
ADD: Book covers & ratings hub in Settings — pick a cover source (Open Library or Google Books), fetch every cover into the offline cache, retry only the failed ones, and fetch ratings with no API key; the reveal now shows a ★ rating chip on books. Chapter chips are compact "CH 1 · CH 2…" everywhere. Fetching is OFF by default — flip the toggle in the hub to start downloading, so the app never uses data without you asking.
FIX: Share card editor — the quick-fact Height and Width sliders now actually resize the box: every tick visibly grows or shrinks it (no more dead-feeling drags or the box hopping around).
ADD: Topic Browser — the floating arrow scrolls smoothly to the top, and flipping a page jumps back to the top too; the "Also in" pills now SWITCH to that category in one tap instead of stacking another filter.
FIX: Spin — the Categories pill shows your mix's actual NAME when you apply a saved mix (no more generic "Mixed").
FIX: Tap-and-hold menus app-wide — the old dialog-style panels (and the Edit/Delete dropdown) are now one small pill with circle icon buttons that pops in with a springy morph animation — no text, no dialog.
FIX: Category mix & filters — saved mixes now use a cohesive dark identity color per mix with a clear selected highlight; cards show real category icons instead of washed-out dots; Cabinet counts + Continue-exploring show the active accent; the Surprise tile/button become Mix while building a multi-lane mix, with a floating Cancel pill.
ADD: Category picker stays where you left it — the Curio / Knowledge / Mix tab and each tab's scroll position now survive closing the sheet and restarting the app.
FIX: Category picker compilation — anchored hold-pill positioning now uses the active density directly and imports the correct offset modifier.
ADD: Cabinet + Topic Browser — the category filter panel now lists categories in a two-column grid, so dozens of lanes are half the scroll.
FIX: Cabinet — the active category chips now look exactly like Topic Browser's and show in every state.
FIX: Topic Reveal — the synopsis on the page is now a tidy 5-line teaser; tap it for the full text in one tall sheet that also holds every chapter (Synopsis | Chapters tabs, switch in place).
FIX: Topic Browser — searching from All now shows "Also in" pills so you can see and jump to which categories matched your search.
FIX: Share card editor — editing takes over the whole sheet (no title overhead), the sliders tick as you snap through sizes, the quick-fact box grows as you type with the caret exactly on the text, and the old T/F/M/B handles are now one coffee-colored move grip.
FIX: Share card editor — the category chip grip now sits on the actual pill wherever it is, and dragging it carries the bulb icon along; rules above the quick fact (and title + byline when together) move as one with their box.
ADD: Settings — the Book covers fetch moved under Experiments with a Cancel button, so you can stop the download early and keep what's cached.
FIX: Topic Reveal — topics you've never opened now show their info instantly too (indexed lookups + a memory index, no second-long wait), and brand-new topics added to the catalog are picked up and saved on-device.
ADD: Cabinet — category filters now use the new panel: tap Categories for a collapsible panel with its own search box and checkbox multi-select, and your chosen lanes show as chips you can remove with one tap.
ADD: Book notes on Topic Reveal — the slim chapter/synopsis dialog is now a full bottom sheet with the book cover, all chapters in a chip row, and in-sheet chapter switching (tap any chip to jump straight to that chapter).
FIX: Topic Reveal — a book's synopsis now reads in full on the page (no more tiny internal scroll box), and the chapter cards are much shorter (they were taller than their 2-line previews).
FIX: Topic Reveal — already-seen topics now show their quick fact and metadata instantly from the warm cache, no more second-long blank flash.
FIX: Topic Reveal — the card morph stays smooth on books with synopsis + chapters (the heavy book content now appears right after the morph settles instead of fighting its frames).
ADD: Settings — a new "Book covers" row fetches every book cover one by one with a live counter (12 / 301…); posters are cached on-device so they show instantly and offline.
ADD: Topic Browser — search no longer shows the full category chip bar; categories live in one place, a collapsed-by-default panel with a tiny search box inside and checkbox multi-select.
ADD: Topic Browser — active category filters show as chips you can remove with one tap, and filtering (text AND selected categories) works while searching too.
ADD: Topic Browser — search now matches typos (fuzzy results) and prioritizes the category your query mentions.
ADD: Satisfying haptics across the app — a confirming buzz when you save, share or land a spin, and light ticks on explore/express-yourself, pinning, opening entries, picks and editor actions.
FIX: Opening a topic never comes up empty — if a topic isn't in the local database yet it's parsed on demand from the bundled JSON and saved on-device, and every explored topic is remembered locally so it stays loaded and never needs reloading.
ADD: Book covers (and all fetched images) now download once and are cached on disk — reopening a book later shows its cover instantly, even after an app restart.
FIX: Share card editor — the Width/Height sliders now show every dimension (Title AND Fact width + height) with clear labels and a live % readout, snapping in clean steps so sizes are precise.
FIX: Share card editor — typing is now accurate: the text caret sits exactly on the card's text and characters land where the cursor points (the invisible edit field now matches the card's font and position).
FIX: Editorial share card — the quick fact now wraps only 2 lines beside the big first letter, with the letter top-aligned to the text.
FIX: Topic catalog loading overhauled — the bundled database experiment is gone; topics now import once from JSON into a persistent on-device cache, so every launch shows all topics instantly (no re-loading) and the catalog survives app restarts.
FIX: Topic reveal card morph is smooth again — the ticket-to-hero expansion uses a critically damped spring, so it no longer snaps or judders on the first frame.
ADD: Share card editor — the category badge/pill is now movable via a B handle in edit mode (every card style).
ADD: Share card editor — Width and Height sliders replace the edge-drag handles for title/fact box sizing.
ADD: Share card editor — Fact size is now a dropdown (alongside the title dropdown) in edit mode.
IMPROVE: Share card editor — edit hint shortened to "Hold to edit"; long description text removed.
IMPROVE: Share card editor — Done and Reset are now icon chips (✓ / 🔄) instead of text buttons.
IMPROVE: Share card editor — Save, Share and Share-as-text are now three compact buttons in one row.
ADD: Share card edits now persist per topic — save or share once and your customisations (move, size, text) restore automatically next time.
ADD: Topic Browser — the selected category now FILTERS search results (searching inside a category shows only that category's matches instead of every category's).
ADD: Topic Browser — dynamic category chips: while searching each chip shows its live match count and categories with no matches hide.
ADD: Topic Browser — searching inside a category shows "Also in" pills for other categories that match; tap a pill to jump the filter there (the search stays).
ADD: Topic Browser — browsing a single category now shows ONLY that category's topics, with an arrow bar at the top (e.g. "← Films · 342 topics") that returns to All.
ADD: Your-mixes cards redesigned as compact "mix stamps" — a leading lane icon, the mix name + lane list and tiny tinted lane-composition dots previewing the mix; tapping a card spins it (the separate inline Spin pill is dropped).
FIX: Category tiles in the picker are now borderless in BOTH themes — the light-mode rings on category tiles, the "+ Add" tile and the playing mix card's Active ring are all gone.
ADD: Mix actions (Edit/Delete) now live behind a tap-and-hold on the mix card — Your-mixes cards and the Browse Mixes tab rows show no 3-dot or action buttons at rest.
ADD: The mix list header drops its "New" pill — the + button on the bottom action row creates mixes.
ADD: The category picker remembers your page and scroll — the page (1 or 2) stays the default as before, and each page's scroll position is saved and restored when you reopen the picker (even after an app restart).
IMPROVE: Your-mixes cards refined — every mix gets a colour identity (a lead-lane tinted cover), the mix currently playing shows an "Active" label, the header shows your mix count, and the empty state is a "Build your first mix" button.
FIX: Selected category tiles no longer show the check tick — the solid category-tint fill alone marks the pick (the old tick read as a pale dot in dark mode).
FIX: Hold on a Pinned pill in the picker now reliably opens the Pin/Unpin + Spin actions — the option-pill overlay was rendering off the sheet's visible area and could show nothing on tall sheets.
FIX: Theme switching keeps its circular reveal when Liquid glass is ON — the frozen frame is now captured from the actual rendered window (PixelCopy) instead of re-rendering the Compose scene, which read back blank over the glass backdrop on some devices and skipped the animation.
FIX: The pale whitish ring borders around category tiles are gone in dark mode — the outline strokes now draw in light mode only (picker pages 1 and 2).
ADD: The "hold for options" hint text is gone from the Pinned section and the pinned rows — holding still works.
FIX: Tapping an attached image on the save page (Reel Notes, Field Notes, Journal, Open Notebook) now opens it full-screen with pinch-to-zoom — the image thumbs used to do nothing when tapped.
ADD: Category picker page 1 is rebuilt as a Curio / Knowledge / Mix mode picker — the Science / Entertainment preset chips are gone, and the page starts CLEAN every time: tap a lane to open it, and tap-and-hold is the only way into mixing, so holding now selects exactly ONE lane (the old page silently pre-ticked your previous deck's lanes — the "it auto-selects 2" bug).
ADD: Selected category tiles in the picker now wear the classic category-tint style — a solid fill in the lane's own color with matching icon and check — and idle tiles and panels lift to a soft cream in light mode instead of dark tan.
ADD: Your mixes grid in the picker shows 6 mixes before the Show all button.
FIX: Continue exploring changes now apply LIVE — ticking or unticking lanes in the Add sheet updates the list instantly, the "hold to remove" hint text is gone, and holding a lane now shows a real Remove action (no more close-and-reopen to see changes).
FIX: Create/Edit mix selection now updates instantly — tapping lanes ticks them on the spot and the Save count follows (the old editor lagged and showed a batch of extra ticks later).
ADD: The first-7 signature cards (Animals, Animated Films, Anime, Artists, Artworks, Astronomy, Authors) swap their tiny drawn crests for a big faint category icon watermark — one unique symbol per category (paw, film frame, sparkle, brush, gallery, pen, moon) in both the normal style and Deepen; the Language card drops its chat-bubble scene for a clean minimal background with its many-language word overlay.
FIX: Category picker no longer crashes on open — the Continue exploring section was nesting a scrollable grid inside the list and hit the same infinite-height error; it now lays out as plain rows.
ADD: Signature share cards for 14 categories (Animals, Animated Films, Anime, Artists, Artworks, Astronomy, Authors, Biology, Books, Chemistry, Directors, Discoveries, Economics, Films) are now truly minimal — the busy background scenes are scrapped for a clean flat gradient, a hairline frame and one tiny category crest, in both the normal style and Deepen; the classic signature designs and Paper/Clean/Collage/Editorial/Minimal stay exactly as they were.
ADD: 7 brand-new fonts added for the second signature batch — BioRhyme (Biology), Fraunces (Books), Oxanium (Chemistry), Limelight (Directors), Rye (Discoveries), Space Grotesk (Economics) and Anton (Films), each matching its category's look.
FIX: Category picker no longer crashes on open ("scrollable measured with an infinite height") — the sheet's classic/new pages and the mix editor measured their scrollable content with an unbounded height; both now fill the sheet's remaining height properly.
FIX: Your mixes grid cards are now uniform height with a bottom-anchored Spin pill, and the 3-dot menu opens as a proper popup instead of shoving the row around.
FIX: Editorial share card quick-fact text no longer overlaps itself — the full-width continuation line was stacking on top of the drop-cap wrapped block; it now flows cleanly below.
ADD: The new signature designs swap their warm cream backgrounds for cool paper-white tones — Animals cool sage paper, Anime cool paper white, Artworks cool gallery white, Authors cool manuscript paper — in both the normal style and the Deepen option; the classic Paper/Editorial designs keep their cream look.
ADD: Signature share cards fully redesigned for Animals, Animated Films, Anime, Artists, Artworks, Astronomy and Authors — each gets its own fresh font and a clean minimal scene (concert-poster spotlight, sage botanical field note, storybook pastel rainbow, rising-sun poster, quiet gallery wall, literary manuscript, star-chart constellation), in BOTH the normal style and the Deepen option; the classic signature designs and Paper/Clean/Collage/Editorial/Minimal stay exactly as they were.
ADD: The share screen is now ONE screen everywhere — the saved-entry Detail page opens the EXACT same share sheet as Topic Reveal (style carousel, Customise panel, hold-to-edit) with this entry's saved Quote / Review (star rating) / Note / mood-board quotes as its source pills, plus "Share as text" in the sheet itself. The Settings Share Hub's Share action is a floating pill that opens the same sheet with your picked design preselected instead of silently exporting.
REMOVE: The old reduced Detail share view is deleted — saved entries share through the same full share sheet as Topic Reveal now.
ADD: Mood board save now has a Quote-boxes add option OUTSIDE the board (your ask: not inside the moodboard) — the below-board Add-quote list with its own paper-style toggle is back, while the on-board floating-quote chip stays hidden; board quotes flow into the saved entry and appear as a Quote source on the share sheet.
FIX: Hold-to-edit resize tabs are finally grabbable and consistent — the width (side) tabs were 6dp slivers that fingers missed and their drag range went dead past full width (the renderer capped lower than the tab's clamp); the height tabs floored line counts so small drags showed no change; and Paper's fact pane overrode the width resize entirely. Tabs are now big luminous bars that stay inside the card, drags follow the card, height rounds to the nearest line, and every FOOTER is fixed — only the author/year row moves with the M handle anymore (no more dragging the \"via Curio\" line).
ADD: Curie's new POP! game — a 10-second round of bubbles floating UP across the screen; tap one (or drag the pet into it) to pop it, but mind the dark prickly ones (they cost a point); big rounds end in confetti.
ADD: Victory confetti burst — when Curie wins a round (finds the hider, you find her, a big star catch or a big POP! round) a tiny burst of colored squares pops over her.
ADD: More Curie dialogue — fresh lines across the spark-chase, hide-and-seek, peek and catch game moments, plus brand-new baby/teen/grown-up voices for the bubble round (start, big-win and "ow, prickly" lines).
FIX: Curie's games can't get stuck after a quick screen change mid-round — the hidden/ghost/round flags were healed after navigation but the game-lock state wasn't, which could silently stop future auto-games; both now reset together.
FIX: Hold-to-edit indicators finally sit EXACTLY on the real text — each card style now reports where its title, quick fact and info rows actually render, so the T/F/M handles, edge tabs and crop outlines wrap the true text on EVERY design (no more floating boxes in the wrong spot). Dragging a box's height tab now truly extends/shrinks the text itself (more or fewer lines show) instead of clipping blank space or shoving the text up and down; every handle and tab stays clamped inside the card so nothing can be lost off-screen, letters ring contrast correctly on any fill, and typing happens right on the card's own fact text.
ADD: Category picker redesign — pinned lanes, saved named mixes, a Surprise me mix button, and a full-screen Browse page with bottom-nav tabs (Browse/Mixes/Pins); the old glass-pill picker stays one toggle away under Experiments.
FIX: Category picker tiles no longer use category accent colours — fills, borders and icons are neutral theme roles in both light and dark; selection reads via a neutral solid fill + check. The in-page Browse/Mixes/Pins nav capsules are solid-filled (never transparent in liquid-glass mode) while keeping the glass edge.
ADD: Tap-and-hold a category anywhere (Browse grid, Pinned row, Pins tab) shows an option pill with Pin/Unpin + Spin instead of pinning directly; pinned pills are taller and wider.
ADD: The category picker sheet now swipes between a classic multi-select page and the new mixes page — pick which one opens first (classic by default). The sheet has no close button (swipe down or back).
ADD: Your mixes show as a 2-column grid (first 5 + Show all); a Continue exploring section below shows your most-spun categories then curated fun lanes (up to 10) you can add or remove.
FIX: Create/Edit mix now shows a clear selected state on each category tile, and the Mixes 3-dot menu offers both Edit and Delete.
FIX: Back from the Browse page returns to the Spin category picker sheet.
FIX: Theme-switch circular reveal now plays in liquid-glass mode (the old software snapshot dropped RenderEffect blurs and came back blank, skipping the animation) and now also plays when toggling the Material theme on/off (a same light/dark-state flip still repaints the whole scheme, so the reveal is forced).
FIX: Editorial share card drop cap no longer overlaps the body text — the big initial sits in its own fixed-width column matched to 3 body line-heights, with the wrapped text beside it and the rest full-width below.ADD: Share cards get MS Word-style text formats — pick a new font (Sans, Typewriter, Display, Elegant) and Left/Center/Right alignment for the quick fact in Customise, applied live on every style and baked into the saved image.
ADD: Editorial share card gets a true drop cap — the first letter stands big and the text wraps around it for three lines, then continues full-width below.
ADD: Hold-to-edit can now MOVE the author/year info rows too — drag the new M handle to shift the byline & footer (they stay read-only, never typed); the title box is now move/crop only with no typing.
FIX: Single-style share sheets no longer ignore your edit adjustments — the preview now applies the same move/format as the exported image.
FIX: Inline edit overlay no longer covers the card text — the title and quick-fact fields are now transparent so you type directly over the card's own text.
FIX: Collage card straight seam between the lighter and darker green is now a natural wavy edge.
FIX: Clean card category glyph is now fully visible (moved left from the edge).
FIX: Style-switch shadow flicker removed — no weird shadow during the swipe between card styles.
ADD: Title text-size dropdown in inline editing (pick 0.75×–1.5× from a small dropdown, not a slider).
IMPROVE: Customise overlay opens at the top, is more transparent, and dismisses when tapping outside; Back also closes it instead of exiting the reveal screen.
IMPROVE: Save + Share buttons redesigned — Save uses a theme-aware tonal container with a download icon; Share uses the theme primary colour.
ADD: 28 BASE signature share-card designs fully rebuilt from scratch (no Deepen) - each gets a clean minimal aesthetic with a newly-matched font: Artists (spotlight poster, Bungee), Wildcard (coral glow, Bebas Neue), Animals (moonlit forest, Cormorant), Animated Films (rainbow arc, Playfair), Anime (sakura, Sora), Artworks (gallery wall, DM Serif Display), Authors (writing desk, Cormorant), Biology (improved DNA helix with A-T/G-C bonds, Space Mono), Books (library, Playfair), Chemistry (hexagon lattice, Space Mono), Directors (clapperboard, Bebas Neue), Discoveries (compass, Pirata One), Economics (chart, Sora), Films (cinema, Bebas Neue), Food (table, Corben), Geology (strata, Sora), History (timeline, Cormorant), Internet (globe, Space Mono), Language (faint multilingual words 言語/Sprache/langue/idioma layered in the background, Patrick Hand), Manga (speedlines, Changa One), Manhwa (dreamy arch, Playfair), Mathematics (golden spiral, Space Mono), Mythology (gold meander, Pirata One), Painters (easel, Lora), Plants (botanical leaf, Lora), Psychology (mind profile, Sora), Quotes (giant marks, Playfair), Scientists (blueprint, Space Mono) - 7 new OFL fonts bundled (Cormorant Garamond, Bebas Neue, Space Mono, DM Serif Display, Sora, Corben, Maven Pro).
FIX: Share sheet Customise menu crash fixed - opening the Customise overlay no longer crashes with an infinite-height scroll error.
ADD: Hold-to-edit is now inline on the card for every style - the title and quick-fact become transparent text fields right on the card (no more separate box panel); drag the T/F handles to move them and drag the edge handle to crop/resize each box like a photo editor; text size stays in the Customise slider.
ADD: Hold-to-edit lets you MOVE the title and the quick-fact box by dragging the T / F handles directly on any card style - the offsets thread through every design (Paper, Vinyl, Collage, Clean, Editorial, Minimal, Signature, Custom) so the title and fact shift live on the card and the saved/shared image matches exactly; Reset clears the placement.
FIX: Quick-fact size now scales the Signature share card too - the fact-size control previously skipped that style; the body text now scales with it like every other card.
ADD: Share sheet Customise revamp - the −/+ stepper rows are replaced with a fact-size slider plus neat chip rows for Design (portrait/classic), Source, Style switching (pick any card design from the panel) and Current vs Classic Signature; the panel sits at the top over a lighter scrim so you can see the card behind it.
FIX: Hold-to-edit text fields now show a clear placeholder so the title and quick-fact inputs always read, even before you type.
ADD: 28 Signature share-card designs redesigned with a quiet minimal aesthetic - cleaner scenes with one focal point each (Artists is a single spotlight with a Bungee poster title, Biology is a scientifically-drawn DNA helix with A-T/G-C base pairs, Language keeps the chat bubbles with a handwritten Patrick Hand title, Discoveries keeps the Pirata One map with a compass focal, Books keeps the lamp-lit library with Playfair Display, Mythology switches to Pirata One gold-on-marble, and more); palettes stay the same, layouts stay the same, typography is re-paired per category.
ADD: Share sheet decluttered - the aspect, source, design-variant and photo rows now live behind a floating Customise button that opens a translucent overlay with −/+ steppers; the quick-fact size control moved in there too and now scales the fact text on EVERY card style, and hold-to-edit works on every style (title + quick fact typed right on the card, Reset + Done below).
ADD: Paper share card hold-to-edit now edits the TEXT itself - hold any Paper card and tap the title or quick fact to type right on the card (the old drag/resize box overlays are gone); the fact-size +/− and Reset stay, and the saved/shared image matches your edits.
FIX: Editorial share card drop cap corrected - the big first letter is now a bold serif initial, baseline-aligned with the body, spanning the first two lines so the third line starts cleanly below it.
ADD: Deepened signature scenes refined to quiet minimal looks (more) - Internet is a clean globe wireframe with three linked nodes and no signal-dot scatter; Biology keeps one helix, one cell and one chromosome; Chemistry trims the lattice to a compact 2x2 sheet; Animals cuts to eight grass blades and four fireflies around the moonlit tree; Plants uses three sunbeams, four leaf veins and two droplets around the botanical leaf.
ADD: Deepened signature scenes refined to quiet minimal looks (more) - Manga is one crisp ink burst and a bold red slash on lighter screentone; Manhwa keeps the dreamy arch and heart with four placed sparkles around it; Mythology drops the marble veins and trims the laurel wreath to five leaves; Sports cuts to four clean field lines with no center-circle clutter; Food removes the scattered crumbs for one basil leaf, plate, bowl and steam.
ADD: More Deepened signature scenes refined to quiet minimal looks - Artworks is one spotlight over a single framed sun-and-hill piece; Scientists drops the blueprint grid for a clean molecule and a three-bubble beaker; Discoveries trims to two contour rings, a short trail and four sun rays around the compass; Series is a clean TV with a simpler mountain scene and no overlay chips; Anime lets eight petals flow on one diagonal under a soft sun.
ADD: Deepened signature scenes refined to quieter minimal looks - Songs is a clean 12-bar waveform with one orb and a single floating note; Directors is one centered clapperboard under a warm key glow; Animated Movies is a single five-band rainbow arc with one star; Authors is a gold-rimmed inkwell with one clean quill arc (no ink splatter); Painters is a calm studio with a simple sky-sun-hill canvas, a three-well palette and no drips.
ADD: Deepened signature scenes upgraded - Books becomes a fuller lamp-lit library (double shelf, floor lamp, armchair), Films gains side curtains with a screen glow and red carpet, Games adds pixel-art easter eggs (mushroom, star, ghost, heart), Artists becomes an aesthetic stage with a light-rig truss, crossing beams and a rim-lit singer (Bungee title), and Albums is a turntable with a tonearm where the record sits clear of the title (Lora serif title).
ADD: Signature card redesigns - Artists becomes a concert poster (twin beams, singer low on stage, big centered title in the light); Albums moves the vinyl to the right with a warm dusk glow, Lora serif title and a clean left text column; Directors turns into a cinema scene with velvet curtain drapes, marquee glow and film credits stacked at the bottom; Painters shifts the easel right so the title column stays clear, with a real framed landscape painting on the canvas and Changa One title.
ADD: Signature Authors/Books card redesigned - a calmer navy writing desk: full-width manuscript lines, a real feather quill with barbs, a gold-rimmed inkwell with an ink pool and splatter, and a Changa One title (was a single hairline quill stroke and a flat inkwell blob).
FIX: Share card crash on launch - the Minimal design's giant watermark used a negative padding value that crashed the app with "Padding must be non-negative"; it now positions with an offset instead.
FIX: Theme switch reveal is more reliable - a blank-frame guard treats empty snapshots as failures so the circular reveal plays cleanly on devices where the frame capture returns nothing (instead of freezing a see-through frame), falling back to the instant flip.
ADD: Paper share card arrange mode - tap and hold any Paper card on the share sheet to move the title and quick-fact box, drag its edge to change the box shape, and scale the fact text with +/−; your layout applies to this share only and the saved image matches the preview.
ADD: Theme switch animation - flipping between light and dark now plays a smooth Telegram-style circular reveal from where you tapped (home sun/moon, the Settings theme picker, or the first-run theme choice); when the flip is a visual no-op it applies instantly.
ADD: Share-card upgrades - Paper gets a grainier paper texture; Vinyl swaps the filler info panel for a small user-set "Favorite song" chip (set on the share sheet); Collage gets a torn seam, a polaroid finish and a retro Bungee title that no longer clips; Clean's glow/depth shadows are now soft; Editorial is a retro broadsheet; Minimal is fully redesigned with Bungee type, a hairline frame, a giant faint category-initial watermark and a bottom-anchored layout.
ADD: Category picker redesigned - three modes (Curio / Knowledge / Mix): Curio is a relaxed culture-first deck, Knowledge groups the deep learning lanes by theme, and Mix keeps multi-select with the quick-mix presets and Wildcard; every lane now renders as a compact quiet icon tile instead of the old large category-color cards.
ADD: Share hub in Settings - browse every share-card design as a full preview grid, search any topic in the catalog, pick a design, and share that topic as a card (9:16 or 3:4, Signature Current or Classic included).
FIX: Save button in the topic share sheet now saves the card image straight to your gallery (Pictures/Curio) instead of opening the share panel - the same Save + Share split now works in the entry-detail share view too.
FIX: Songs share-card waveform restored to solid bars with a compact vertical span (rather than thin slivers), and the Albums vinyl sits further down and to the right.
ADD: Every category gets its own signature share-card design - 35 unique researched palettes and hand-drawn backgrounds (stage spotlights for Artists, vinyl for Albums, sakura petals for Anime, gold Greek-key marble for Mythology, ocean light-rays for Oceans, golden spiral for Mathematics and more); Films, Books and Games keep their classic look.
ADD: Deepen signature card elements - an opt-in Experiments toggle swaps signature share cards to richly detailed layered scenes: gradient atmospheres, glows, vignettes and hand-drawn art per category (spotlight stages, turntable vinyl, EKG traces, galaxy spirals and more); Films, Books and Games keep their classic look.
FIX: Deepened signature card scenes refined - chemistry returns to a connected hexagon lattice (no more off-looking flask), gallery frames sit below the title so text stays readable, expedition trails lead into the compass, laurels anchor to the temple, and shapes keep their proportions on both 9:16 and 3:4 cards.
ADD: Classic signature share-card designs restored - pick "Classic" beside "Current" in the share sheet to use the original family-based designs (vinyl for music, gallery frame for art, Greek columns for mythology and more) for any category; Films, Books and Games keep their classic look either way.
ADD: Share hub now shows every per-category signature design as its own pickable cell - browse the Artists stage, Albums vinyl, Films cinema, Books library and every other signature background, and apply any of them to any topic.
ADD: The Spin page's category picker now opens the redesigned Curio/Knowledge/Mix picker (compact icon tiles) as a smooth inline sheet above the shuffle page.
FIX: Signature share-card overhaul - every category now has a UNIQUE text layout (badge/title/body positions vary per category instead of identical placement), and the worst backgrounds were redesigned: Games gets a neon arcade with glowing D-pad + A/B buttons + coin, Psychology gets a head profile with neural network, Artworks gets a spotlit museum gallery wall with bench, Animals gets a moonlit forest with fireflies.
FIX: Collage polaroid caption field is no longer squished - proper height shows the text you type.
FIX: Collage polaroid photo box is tappable again - tapping the empty "Tap to add photo" frame opens the photo picker instead of swallowing the tap (and no longer fights the card swipe).
FIX: Category picker tiles are theme-aware - selected tiles wear their category accent (not a generic secondary color), the sheet has a glass grabber handle and a centered title with a glass close chip.
FIX: Quote share cards no longer show the quote twice - Vinyl, Collage, Clean, Editorial, Minimal, Signature and Custom now put the author in the title and the quote in the body (Paper was already correct).
FIX: Collage polaroid caption no longer squished or clipped - the handwritten credit ellipsizes cleanly with proper line height on any aspect ratio.
FIX: Signature share-card backgrounds polished - Artists gets a full stage scene (singer at the mic, spotlight beams, monitor wedge, falling confetti and music notes), Painters an easel with a half-painted canvas and kidney palette, Oceans a sun shaft with a fish school and coral bed, Series a TV playing a scene with episode chips and a progress bar, Animated Films a shared-center rainbow swoosh with a star trail, Films now shows two prominent film strips with sprocket holes, and Manhwa a dreamy arch with a floating heart.
FIX: Chemistry share card without deepen now uses a connected hexagon lattice (no more scattered tiles and broken flask), and the Songs soundwave bars are thinner and more elegant.
ADD: Books, Films and Games now get rich detailed backgrounds with the "Deepen signature card elements" experiment on - a cozy lamp-lit library with stacked books for Authors/Books, a cinema marquee with velvet curtain and film reel for Films, and a neon arcade with glowing coin and controller for Games.
FIX: Topic share card styles polished - Vinyl text is cleaner, Paper 9:16 facts fit better, Clean gets a category-depth poster, and Collage has a natural tear with editable polaroid credit.
FIX: Room topic migration crash - repairs the topics table defaults and removes undeclared legacy indexes while preserving existing topic data.

FIX: Liquid glass crash recovery - auto-disables glass after 3 consecutive native crashes on budget devices (Infinix/Samsung on Android 16) to prevent SIGKILL on RenderThread.

FIX: Tab switch animation now snaps instantly to the new tab on tap - no more sideways glide that looked glitchy especially on Cabinet.
FIX: Pet Designer nav blob hold-and-drag now tracks the finger without spring lag - the page switches correctly when releasing on a different tab.
FIX: Nav indicator text in Material light mode uses pastel fill ink instead of white-on-white.

FIX: Share card export no longer has white background or rounded corners - the image now matches the gradient card exactly.
FIX: Pet Designer nav bar active indicator now shows tab labels at rest again.
FIX: Category chips in Cabinet and Topic Database always show frosted glass even when Clear Glass is enabled.
FIX: Share card preview is now centered and full-size - the exported image matches the preview exactly, no more scaling mismatch.
IMPROVE: Splash screen is faster - shorter delays, quicker catalog warm-up, so the app opens faster.
IMPROVE: Category chips in Cabinet and Topic Database now have full Samsung frosted glass - stronger frost wash over blurred backdrop.
ADD: Entry detail shares join the same share hub as Topic Reveal - your saved Quote, Review (with its star rating) and Note appear as options based on how you captured it.
FIX: The shared card image now saves exactly what you see in the preview - same layout math, text sizes and placement.
IMPROVE: Share cards redesigned - your category's own icon tiles the watermark, a cleaner gradient backdrop, a softer frost pane and a "via Curio" footer that never clips.
IMPROVE: Review rating helper names your stars - "Not for me" through "Loved it" - instead of the generic "Rate quality" prompt.
FIX: Detail page back/more bar aligned to match other screens' hero controls - no longer floats below the expanded menu.
FIX: Pet Designer nav bar duplicate text on hold is gone - the crisp ink overlay is hidden in ghost-free mode.
FIX: Category chips restored to clear liquid glass - milky frost boost reverted so cabinet and topic-database chips refract properly.
IMPROVE: Fire widget icon enlarged (26dp -> 36dp) and streak count text larger for better visibility on the home screen.
IMPROVE: Clock widget margin reduced (8dp -> 4dp) so the analog face fills more of the tile.
ADD: Topic share cards - tap the new Share pill on Topic Reveal to send a beautiful frosted card of any discovery: 9:16 story or 3:4 classic layout, with the quick fact, your own custom fact or your review.
REMOVE: The lab live wallpaper service is fully removed from the app - no more dead code behind the gone "Set as live wallpaper" button.
ADD: Two new home-screen widgets from the Glass Widget Lab - a round frost analog clock with live ticking hands and a fire streak tile (launcher > Widgets > Curio).
ADD: Nav indicator colour option - Auto follows your theme (Material, azure or rose), with White and Black as fixed picks.
ADD: Indicator opacity slider - the frosted active nav pill's transparency is now customizable (Appearance), while the touch glass effect stays untouched.
FIX: Nav active pill is frosted glass at rest again wearing your indicator colour, with the touch capsule effect when pressed and iOS-fast tab taps.
FIX: Category and topic-database chips are extra frosty - deeper frost blur and a much stronger milk wash on small pills.
REMOVE: The "Set as live wallpaper" button is gone from the Glass Widget Lab.
ADD: 150+ new real topics across Mythology, Series and Anime - deeper catalogs with curated teasers and explore actions.
FIX: Lab live wallpaper - the base wallpaper stays full-resolution and sharp (no more whole-screen blur), with true gaussian frost behind each pane.
FIX: Liquid glass credit now points to Kyant's AndroidLiquidGlass.
FIX: Lab live wallpaper now uses true gaussian frost (the same blur algorithm as the in-app lab), and a decode bug that made your chosen wallpaper vanish into a gradient is fixed.
FIX: Widget corner radius now rounds the whole widget on every launcher, resizing re-renders the fill correctly, and the pill can shrink to 2 cells.
FIX: Lab live wallpaper fidelity - smooth non-pixelated frost that matches the lab, per-widget blur strength, rim and text clipped inside each pane, real battery icon (no more leaked text), and fonts matching the lab exactly.
FIX: The lab live wallpaper now uses your chosen wallpaper (or the device wallpaper on auto), keeps your exact widget placement, and the baked widgets show live data - real clock, session timer, streak, battery.
ADD: The glass widget lab remembers your layout, sizes, blur and colors between visits.
FIX: Glass widget settings redesigned - compact content pills, one accurate preview over a neutral backdrop, and a roomy custom color picker.
FIX: Lab widgets are back to their proper sizes with correct text scale; the checkmark tile is gone.
ADD: Per-widget hide in the lab - select a widget and tap Hide, or Hide/Show all at once.
ADD: Corner roundness slider for the glass widget pane - from subtle 8dp to near-circle.
ADD: Edit home screen widgets from inside the app - works even on launchers without long-press Edit support; Apply updates the home widget instantly.
ADD: Lock screen mode - only the clock shows while locked, everything else pops back with a soft fade when you unlock.
ADD: New analog clock widget with live ticking hands in the glass widget lab.
ADD: Glass Widget Lab overhaul - hide/show all floating widgets, tap any widget to select it and edit its size, liquid-blur strength and text color, plus real time, live explore-session timing, a streak ring with fire icon, battery and date widgets.
ADD: Save your lab composition as a live wallpaper - your wallpaper with your frosted widget layout behind every page.
FIX: Custom widget color gets a real picker - a saturation/value square plus hue bar, with opacity as its own separate slider.
FIX: The widget preview is now an accurate miniature - same gradient math, icon tile and text lines as the real widget.
FIX: Glass widget style options are selectable again and the whole editor scrolls properly.
ADD: Widget content is richer - a mode icon tile plus two-line text (title + detail) instead of one plain string.
FIX: Glass widget styling is back - a live preview shows exactly how your pane will look as you pick Blur, a preset or a custom color.
FIX: The Samsung wallpaper blur on the glass widget works again - and a new Blur option keeps that pure look; the harsh white border is gone.
ADD: Glass widget pane styling - pick a preset (Light, Dark, Clear, Rose, Sky) or go fully custom with color and opacity sliders, per widget.
ADD: Glass widget lab - new One UI frost tile to compare against the liquid-glass shapes.
ADD: Glass widget customization - long-press the widget and tap Edit to choose what it shows: Streak, Quests, Cabinet or Sessions, with a light or dark frost pane.
ADD: Glass widget lab auto-detect now offers All-files access so the real wallpaper can be read on Android 13+.
FIX: The glass widget keeps its rounded corners on all launchers.
FIX: Material theme now restyles the Spin filter + category picker heroes and their pills with the shared primaryContainer tear look instead of the category tear.
FIX: The detail hero under Material hero tears wears the same airy container color as Home/Profile (was a dark saturated block), with matching ink.
IMPROVE: Turning Material theme on now auto-enables "Material hero tears" so the whole banner family switches with it.
REMOVE: The "Glass parallax tilt" experiment is gone - the gravity sensor no longer runs for it (performance/heat win; the feature never worked well).
REVERT: The session screenshot watcher is withdrawn (permission + plumbing removed).
NEW: Glass widget - add a frosted liquid-glass tile to your home screen showing your live streak; tap it to open Curio (long-press your launcher > Widgets > Curio).
NEW: Glass widget - REAL wallpaper frost on Samsung One UI 7+: the launcher now blurs your actual wallpaper behind the widget (other launchers keep the frosted tile).
ADD: Glass widget lab - Auto-detect button tries every system wallpaper-reading strategy, and your picked image now persists between visits.
FIX: The Glass widget lab now renders REAL refraction unconditionally on Android 12+ - the shapes use the raw nav-bar glass recipe directly instead of a toggle-gated path that could fall back to a flat pane.
FIX: Real blur on older devices now detects blank readbacks and retries before falling back - no more pills that look transparent with no blur.
FIX: The explore bubble no longer shifts after expanding and no longer flickers mid-morph (window stays anchored; animations can't overshoot anymore).
ADD: Glass widget lab - a "Set wallpaper image" picker, since auto-detecting the system wallpaper fails on many devices.
ADD: The floating pet got richer life outside the app - periodic play-bow hops when idle and a double-tap spin.
IMPROVE: The explore bubble expands noticeably snappier - the open morph settles in ~150ms instead of drifting open.
IMPROVE: Home & Profile floating pills are now liquid glass from rest when Liquid glass is on - no more solid hero shade, so the dark mid-scroll state is gone for good.
REVERT: The nav tab glyph crossfade is undone - the icon swap is instant again, exactly as before.
NEW: Glass widget lab - drag REAL refracting liquid-glass widget shapes (clock tile, timer pill, glyph tile) over your actual wallpaper to test future widget designs.
FIX: The Home and Profile floating pills no longer show a dark muddy shade mid-scroll - the solid-to-glass morph now commits to clear glass fast instead of fading a dark fill across the whole scroll range.
STYLE: The explore bubble reads as a raised 3D bubble now - painted drop shadow, domed light-to-shade fill, a broader category glow ring on the small pill, and a domed sheen + hairline rim on the expanded panel.
ADD: Real blur on older devices - an opt-in Experiments toggle brings REAL frosted glass to pre-Android-12 phones for the first time: an app-side blur engine draws the actual content scrolling behind the nav bar and Topic Reveal pills (no static fake veil).
IMPROVE: Explore bubble drag is rock solid - the pill clamps against the screen edges live while dragging (no more rubber-band snap on release), and the expanded panel moves only from its header row so buttons and the note field never fight the gesture.
IMPROVE: Explore bubble expand feels springy and tactile - one shared spring shapes the panel pop-in, plus a gentle press squish on the pill while touched.
FIX: Floating pet now plays its full animation set outside the app - walk bob + lean + tail wag while it wanders or rides your drag, a lifted startled pose while held, and a dizzy recovery wobble right after you put it down.
- Glass Lab: widgets show real icons/text again (clock, analog hands, streak fire, battery, date)
- Glass Lab: frost tile now hides with per-widget Hide
- Live wallpaper: panes render real baked glass - aligned blur + vibrancy of the wallpaper behind each pane, gloss + rim
- Live wallpaper: text/icon sizes fixed to match lab dp sizes exactly
