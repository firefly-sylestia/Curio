Saved entries now pop up from the center of the screen, and the blank strip at the bottom is gone. New weekly quests: three rotating week-long goals with a fresh mix every Monday. Your pet now has a memory — it learns your habits and coins its own catchphrases (toggle in Settings → Appearance). The Pet Designer is now a clean studio: pick what to edit on Animations, Actions and Settings pages, with Save always visible. Custom colors now render exactly right — the blue channel no longer gets lost, so swatches and the drawing canvas show your true color. Drawing got simpler too: pick a tool and start painting (no more draw switch), with Undo/Redo always available under the editor. The color picker went pro: before/after previews, a draggable hue strip, and your recent colors are remembered. The Animations page now has a looping preview gallery and a frame timeline — watch your pet hop, spin and celebrate, then tune each frame in fps with playback speed. The whole studio got a refresh: a sticky 'Draw & switch' strip with a preview picker dialog (name-tagged cards), darker locked blueprints, and a smarter save bar with Undo/Redo icons and an 'Unsaved changes' indicator.
